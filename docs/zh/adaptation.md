# 平台适配经验

moonbit-libyue 在各平台适配过程中的实测经验与坑,全部来自真实环境验证。
主 AI 文档(仓库根 `AGENTS.md`,ZCode 读取)经 `@docs/adaptation.md` 引入本文件;以本文为平台适配坑的持续更新详版(README 只留指针,不再展开)。

**组织维度**:

- Linux 按「发行版 → 桌面环境 → 版本」三层;
- Windows / macOS 按「版本」。

**状态标记**:

- ✅ 实测通过
-  ⚠️ 部分可用或带条件 
-  ❌ 不可用
-  ❓ 未实测(占位,待补)

---

## 跨平台通用(构建链 / FFI,不分平台)

### 构建与链接

- `moon` 命令必须在**仓库根**执行的习惯保留:链接参数虽已改为绝对路径(见下条),但 vendor/build 产物与 WebView2Loader.dll 的运行期搜索仍按工作目录。
- moon 的 `link` 段只作用于所在包、只对 main 包的二进制生效;库包(如 `yue/`)放 link 段会让 moon 生成无 main 的 `.exe` 导致构建失败。链接配置已全部收敛到 `scripts/prebuild.py`,任何包的 `moon.pkg` 都不再写 `cc-link-flags`。
- **manifest 迁移到 `moon.mod` / `moon.pkg` 新格式(2026-09-15 实测入档)**:moon 新版弃用 `moon.mod.json` / `moon.pkg.json`,`moon fmt` 一键迁移(字段改 DSL 风格:`preferred_target`、`options(...)` 块;pkg 的 import/targets 同步迁移);`moondoc` / `moon doc` 只认新格式——mooncakes 0.1.0 的「Documentation failed to generate」即服务端 moondoc 找不到 `moon.mod` 所致,迁移后发新版本号即恢复。注意:旧版 moon(stable 20260904 等)不认新格式,消费方需较新工具链。
- **双平台链接参数由 pre-build 钩子传播(当前方案,2026-09-12 实测入档)**:moon 的 `cc-link-flags` 是单一字符串、`targets` 条件编译无 OS 维度,链接参数无法按平台入库。现行机制:`moon.mod` 声明 `--moonbit-unstable-prebuild: scripts/prebuild.py`,moon 每次构建执行它,脚本按 `platform.system()` 输出 `link_configs` JSON,moon 把它**自动传播给所有依赖 yue 包的 main 包**——本仓 examples 与 mooncakes 使用方统一零配置,切换平台自动换参数。实测要点:
  - 脚本 **stdout 只能是最终 JSON**:任何 print(含子进程透传)都会导致 moon 反序列化失败(`invalid number at line 1 column 2`),进度信息一律走 stderr。
  - **不要用 `link_libs` 放 libyue_mbt**:moon 组装命令行时 `link_flags` 在 `link_libs` 之前,GNU ld 从左到右解析,`-lyue_mbt` 排在 `-lstdc++` 之后会 `undefined reference to __cxa_guard_acquire`。全部参数放 `link_flags` 单一字符串自控顺序(与旧响应文件同序)。
  - 脚本 cwd 是 **moon 调用目录(使用方项目根)**,定位自身必须 `Path(__file__)`;传播的库路径必须是**绝对路径**(链接命令的 cwd 在使用方侧)。
  - 原生产物缺失时脚本自动调 prepare.py 补建(产物存在时毫秒级返回),首次构建走 GitHub 下载。
  - 该机制官方标注实验性(`--moonbit-unstable-prebuild`),API 可能随 moon 升级变动;Windows 下 `python3` 命令可用性待真机验证。
  - 历史方案:先按系统回写 moon.pkg.json(两平台互相覆盖)→ `@build/link.flags` 响应文件(gcc/clang 与 cl 都支持 `@文件`,双平台同一份入库)。均已被传播机制替代。
- `prepare.py` 幂等可重跑:缓存 zip sha256 不匹配(下载被截断)自动删除重下;下载先写 `.part` 临时文件、校验通过才原子落盘。网络走标准 `http_proxy/https_proxy` 环境变量。
- **moon 不因静态库更新自动重链(跨平台,2026-09-15 Linux 实测)**:prebuild 只在静态库**缺失**时才调 prepare,且 moon 的重链判定只看 MoonBit 源与 link_configs 输出、不看静态库内容——shim/vendor 变更重跑 prepare 后,链接产物仍指向旧库。判别与处理见 [docs/relink.md](relink.md)。
- **shim 新增函数的三种死法(2026-09-16 实测,Linux 侧修复)**:「方法级审计补齐」批次(9-16)新增的约 90 个 shim 函数在 Linux 侧全部未编入,任何 MoonBit 调用即链接失败。三个叠加原因:①整段函数被错误圈进 `sys_*` 平台分支的 `#else(非 Linux)` 块(该段本应只含 sys 桩);②函数定义未写 `extern "C"`,符号被 C++ name mangling 改名,ffi 按 C 名解析找不到;③`yue_mbt.h` 的 `extern "C"` 块在 602 行被提前 `}`,605-697 的新声明全在块外(C++ linkage),与 cpp 侧定义冲突。段内另有六类真实编译错误被「挪进非 Linux 段」掩盖:Window 焦点信号名(`on_focus`/`on_blur`,lambda 参数为 `Window*`)、MessageBox `set_informative_text` 重复定义、`Table::Notify*` 是私有 API(GTK 端模型自动通知,Linux 桩 no-op)、`Appearance::SetDarkModeEnabled` 为 Windows 独有、审计段误用 `CastTo<>` 应为对应 `XStore::get()`(Linux 端部分类无 `kClassName`)、`base::JSONWriter` 需显式包含 `base/json/json_writer.h` 且 `ValueView` 按引用构造。修复:`#endif` 收窄到 sys 桩结束、统一 `extern "C" {}` 包裹审计段、头文件闭合点挪到文件尾、六类错误逐一改正。**新增 shim 函数的门槛:Linux 全量编译通过 + 声明进 `yue_mbt.h` 的 extern "C" 区 + `nm` 确认符号以 C 名字存在(出现 `_Z` 前缀即缺 extern "C")**。验证:修复后 Windows 首次全量编译通过,showcase/组件库真机运行正常(2026-09-16)——六类错误正是首次 Windows 编译逐个暴露的。
- **MSVC 按 cp936 误读无 BOM UTF-8 源码,报「意外的 #endif」类假错误(2026-09-16 实测)**:ACP=936(GBK) 的机器上,中文注释的双字节序列会把后续字符(如下一行行首的 `#`)吞进字对,预处理指令被破坏;报错行号处根本没有 `#endif`。修复:CMake 对 MSVC 加 `/utf-8`(源码与执行字符集均按 UTF-8)。此坑对任何含中文注释的无 BOM UTF-8 源在中文 Windows 上必现,与代码内容无关。
- **shim 拖拽辅助函数把 GLib API 泄漏到 Windows(2026-09-16 实测,首次 Windows 编译审计段时暴露)**:`MakeFilePathsForDrag` 无平台保护地用了 `g_get_current_dir()`(GLib 仅 Linux 有)。修复:拆出 `CurrentDirForDrag`(Windows 用 `GetCurrentDirectoryA`)与 `IsAbsPathForDrag`(Windows 判盘符 `C:` 与 `/`、`\`),分隔符按平台给。教训:审计段/新增 shim 函数里引系统库前先确认该库在目标平台的可用性,GLib 系(`g_*`)是 Linux 专属。
- **shim 加新 FFI 后静态库过期,链接 `undefined reference to yue_mbt_*`(跨平台,2026-09-16 Linux 实测)**:shim/FFI 源码更新后未重跑 `prepare.py` 时,旧 `build/libyue_mbt.a` 缺新符号,用到新 API 的程序链接失败——2026-09-16 实测:shim 9-16 加入 `get_bounds_in_screen` 系列/`label_set_align`/`view_set_visible`,库停在 9-15,`moon run examples/showcase` 直接链接失败。两个加重因素:① prebuild 只在库**缺失**时才补建,库存在但过期不重编;② hello 等小示例不引用新符号,链接成功造成「构建正常」假象,必须用 showcase 验证。曾误判为 moon nightly 0911 的 test 构建回归(nm 对比库与 shim 符号后纠正)。修复:`python3 scripts/prepare.py` 重编 + `moon clean` 强制全仓重链。判别:`nm build/libyue_mbt.a | grep <新符号>` 零命中即库过期。同族坑见上条与 [docs/relink.md](relink.md)。
- **旧静态库静默吞掉 shim 修复,真机复验"修复无效"(跨平台,2026-09-16 Windows 实测踩坑)**:Windows 修复(set_borderless 反逻辑、on_scroll 推迟等都在 shim)推送后,用户侧 `git pull + moon clean + moon build` 测试仍复现旧缺陷——`moon clean` 只清 `_build`,`build/yue_mbt.lib` 是旧库;prebuild 的「缺失才补建」判定放过它,重链的仍是修复前代码。修复:prebuild.py 加 `_shim_newer_than_lib` 守卫——shim 源码(`shim/*.cpp|*.h`、`shim/include/*.h`)mtime 比静态库新时,自动 `cmake --build` 增量重编(无需重下 vendor/重跑 prepare),并提示删除 `_build` 下 exe 触发重链。此后 shim 改动 `moon build` 即自动生效,仅 vendor/prepare.py 补丁变更仍需手动跑 prepare.py。
- **`postadd` 脚本仅在 registry `moon add` 安装时触发**,path/git 依赖与模块自身构建不触发;产物缺失的兜底由 prebuild.py 的检查承担。vendored 库上线后 `postadd` 已移除:registry 安装直接用随包二进制,无需任何安装期动作。
- libyue 版本钉死在 `scripts/prepare.py`(`LIBYUE_VERSION` + 六个 sha256:三平台源码发行包 + 三平台预构建库),升级需同步更新校验和。上游已从 yue/yue 切到 fork lb091188/yue(见下条)。
- **上游切换到 fork(lb091188/yue)+ 预构建资产管线(2026-09-18 实测入档)**:原方案下载官方 yue/yue 发行包后本地打 13 组 vendor 补丁,升级/复现链路重;现全部补丁以独立提交进 fork(main == 上游 v0.15.6),fork CI(`prebuilt.yml`,tag `v*-mbt*` 触发)产出六份资产:三平台源码发行包 + 三平台预构建静态库(linux-x64 / mac 通用二进制 lipo / win-x64),release 直接发布(草稿资产匿名不可下载)。prepare.py 改为「预构建优先、源码回退」:`LIBYUE_FORCE_SOURCE=1` 强制源码;无资产平台(linux/arm64)自动回退;`build/prepare_stamp` 记「版本 模式」,prebuild 检查过期防误链旧产物,源码模式会清掉预构建残留库防双份符号。win 发行包由 fork 捆绑 WebView2 SDK 头+loader DLL(上游只带裸 DLL),NuGet 补齐逻辑整体移除。坑:fork 侧 `no_destructor.h` 笔误在**构建期**就会炸 mac(新 Xcode clang 实例化该模板成员),修正必须进 `bootstrap.js` 子模块检出后立即打(打包期再补太晚);win 子模块检出为 CRLF,补丁匹配前先归一化行尾;大小写不敏感卷上 `webview2.h` 别名与 `WebView2.h` 同文件,复制前用存在性探测跳过。
- **clang 产物 libyue + gcc 编译 shim 混链,运行期稳定段错误(2026-09-18 Ubuntu 24.04 实测,预构建管线的核心坑)**:现象:fork CI(ubuntu-22.04)出的预构建 `libyue.a`(clang 编)接入后 `moon run examples/hello` 在 `moonbit_main` 第 10 行 `set_content` 处 5/5 段错误,栈停在 `gtk_container_add ← nu::Window::PlatformSetContentView`;首次运行偶发优雅退出更具迷惑性。对照实验(同机同源码,唯一变量是编译器):gcc 库+gcc shim ✅ / gcc 分离布局+gcc shim ✅ / clang 库+gcc shim ❌ / clang 库+clang shim ✅——**静态库与 shim 必须同编译器家族**,跨家族(即使同为 Itanium ABI、同为 libstdc++)在 libyue 这份代码上必崩,根因未深挖(疑头文件内联代码跨编译器布局/行为的隐性分歧)。修复:fork `prebuilt.yml` Linux job 不再把 cc/c++ 切到 clang(该步承袭上游 CI,上游全链路 clang 无此问题),预构建库用 gcc 出;mac(clang/clang)与 win(MSVC/MSVC)天然同族无碍。另:22.04 工具链产物最终链接需显式 `-latomic`(`__atomic_store`,官方 dist CMakeLists 本就链 atomic),prebuild.py 的 Linux 链接参数已补。
- **vendored 二进制随 mooncakes 分发(2026-09-18 起,仿 justjavac/quickjs)**:`lib/{linux-x64,macos-universal,windows-x64}/` 内含 shim 库 + 预构建 libyue(+win 的 manifest.res 与 loader DLL),prebuild.py 级联「vendored → build/」:vendored 就位即零 C++ 编译,shim 源码比 vendored 库新时自动回退 build/ 开发流程;三平台产物由本仓 `vendor-native.yml`(tag `vendor-*` 触发)分平台固化、以 release 资产汇聚,维护机 `python3 scripts/vendor_native.py --fetch <tag>` 拉齐提交。开发链路(prepare/源码回退)不变。
- **moon 工具链 2026-09 起把「trait 方法隐式提升到类型」全面标记 deprecated(2026-09-19 实测入档)**:此前 54 行伏笔的 `implicit_impl_as_method` 收紧已在 `moon test`/`moon build`(native 全量编译)路径落地——`moon check` 不报、增量编译不重报,clean 全量跑才见全量:仓库 `obj.method()` 形式的 ViewLike 隐式调用 488 处全数报警(此前"check 0 警告"的提交门槛因此漏过)。修复:全仓批量迁移为显式静态调用 `ViewLike::method(obj, args)`(493 处:脚本 486 + 手修 7;`obj.(ViewLike::method)()` 限定形式在该版本反报 Type Mismatch 不可用)。**新写组件代码一律直接用 `ViewLike::xxx(obj, ...)` 形式**;批量迁移脚本的正则不识别 `arr[i].method()` 索引 receiver(错切 5 处手改),同型迁移再跑需补索引形态。验证:clean 后 check/test/build 三路零警告、33/33、双示例冒烟存活。
- **改 shim 不重出包 → vendored 落后一代;git 检出 mtime 恒误判回退(2026-09-19 Win10 实测)**:现象:`1cdef9d`(MouseEventScreenPoint,鼠标蹦床 7→9 参)只改 shim 未重跑 vendor 出包,三平台 vendored 库停在 `56aa944` 落后一代——MoonBit 层已按 9 参 extern,vendored 库仍是 7 参旧符号,零编译路径链接必 undefined reference;且 git 克隆/拉取按**路径序**写盘,`lib/` 恒早于 `shim/` 落盘(毫秒差),`_sources_newer` 的 mtime 比较在**每次拉取后**都误判「开发者在改 shim」回退 build/ 重建流程——普通 shell 无 MSVC 环境直接卡在 CMake 的 INCLUDE 拦截。回退方向反而躲过了断链旧库,但 Windows 重建又暴露该提交的 GTK 符号漏守卫(见 Windows 节 2026-09-19 条)。修复三层:①shim 两处定义补 `OS_LINUX` 守卫;②`_sources_newer` 改 **git 判新**:shim 有未提交改动视为新,否则按 `shim` 与 `lib/<平台>` 的最后提交时间比较(非 git 环境才回退 mtime 比较);③vcvars64 环境跑 `vendor_native.py` 回填 `lib/windows-x64`(linux/mac 仍落后一代,待 `vendor-*` CI 出包后 `--fetch` 拉齐,期间 Linux/mac 消费方自动走源码回退路径)。教训:**改 shim 的提交必须同批回填至少本机平台的 vendored 库,并尽快走 CI 拉齐三平台**,否则「零编译即用」对用户是断链的。验证:普通 shell(无 MSVC 环境)`moon check` 零警告、`moon test` 全过、`moon build examples/showcase` 零 C++ 编译直链 vendored 通过,showcase 冒烟存活。

### MoonBit cfg(platform=) 平台条件编译

- **现状(2026-09-14 实测入档)**:moonc 已实现 `#cfg(platform="windows"/"linux"/"macos")` 条件编译(官方文档未写),求值依据是传给 moonc 的 `-target` **三元组**(`x86_64-unknown-linux-gnu` / `x86_64-pc-windows-msvc` / `aarch64-apple-darwin`);`not/any/all` 组合器可用。moonbitlang/openseek 生产在用(仅 `platform="windows"` 及其反)。
- **但当前可安装的 moon(stable 0.1.20260904 与 nightly 0.1.20260911)构建时只给 moonc 传无 OS 信息的泛用 `native`**,所有 `platform=` 条件恒 false——Linux 上连 `platform="linux"` 都不命中;`MOONBIT_NEW_NATIVE=1` 与 `moon.mod` 新格式(直出目标文件后端已生效,产物为 .o)也不传三元组。moon main 分支已有按宿主选三元组的逻辑(`NativeTarget::from_host`:x86_64 Linux / Apple Silicon 默认启用、Windows 需 env=1),**待发布版本携带后条件才会点亮**。
- 迁移信号:任意构建加 `-v`,moonc 命令行出现 `-target x86_64-unknown-linux-gnu` 即可用;届时 showcase 的运行时平台段可一行替换为 `#cfg`。
- 当前替代:运行时 `platform()` 判断——声明式树按条件组装节点,不满足就不创建控件,效果等同编译期隐藏;showcase 的平台段(桌面环境、剪贴板主选区、自定义协议等)即此方案,见 `examples/showcase/section.mbt` 头注释。
- 验证方式:临时工程双分支(`#cfg(platform="linux")` / `#cfg(not(platform="linux"))`)编译运行看走哪支;moonc 直调带 `-target x86_64-unknown-linux-gnu` 可证编译器侧已生效(实测三分支各归各位)。另:`moon.pkg.json` 的 `targets` 文件级条件仍只有后端(wasm/js/native)+ debug/release 维度,喂 OS 值直接 schema 加载失败;裸标识符条件(如 `#cfg(linux)`)恒真,无意义。
- 顺带:nightly 0911 对全仓 `impl ViewLike for X` 报 19 处 `implicit_impl_as_method` 弃用警告(stable 0904 无),属工具链前向收紧,非库代码回归。

### 声明式层与自绘组件

- **mouse_enter/leave 回调里 set_background_color 改写运行期 CSS,会吞掉紧随的首次 press(2026-09-19 真机实测入档)**:backtop_t 曾在 hover 回调里 set_background_color 表达 hover 底色(GTK 端实现为 ApplyStyle 加 CSS provider)——真机现象:滚动到底后第一次点击「纹丝不动」,第二次点击才回顶;即首次 press 被事件窗口扰动吞掉。全库其余自绘件的 hover 都是 on_draw 现取色板(backtop 是最后一只漏网)。修复:hover 底色改 on_draw 表达(hover 态画深主色),enter/leave 仅置状态 + schedule_paint,零 CSS 改写。**规范:运行期(mouse 回调内)禁止 set_background_color,hover/按下等交互态一律 on_draw 表达;set_background_color 只允许出现在挂载期与主题订阅回调里。**另:「点两次才生效」类报障先分清「纹丝不动」(事件层,查 press 被吞)与「闪回又弹回」(设值被在途滚动事件拉回,用 set_timeout 短延时复设兜底)两种形态,修法不同。
- **Container 子项的挂载序 = flex 排列序 = z 序,夹层件(把手/分隔)必须严格按排列位置挂入(2026-09-19 实测入档)**:Splitter 首版把 handle 先于两栏 attach(实现中途调整挂载点位置所致),排列序变成「把手、次栏、首栏」——把手被排到容器最左缘的 8px 窄条,视觉上「中间没有分隔条」,用户视角即「拖不动 + 找不到拖动区」;事件系统本身完好(点按/拖动逻辑都在,只是把手不在用户以为的位置)。修复:严格 pane_a → handle → pane_b 的挂载顺序,并把把手视觉改为常显(中央 1px 分隔线 + 双列点纹,不依赖 hover),hover 浅灰底、拖动主题色底白点。**教训:①flex 容器的 attach 顺序绝不能当「仅影响 z 序」来调整,它同时是排列序;②可交互的自绘夹层件必须常显视觉,不能靠 hover 才现形——用户找它的方式是「看」不是「碰」;③排查此类问题用探针示例 + xwd 截图看真实渲染布局最快,布局错乱一截图就现形(xdotool 合成拖动与 GTK motion 识别不兼容,拖动类交互以真机为准)。**

### MoonBit ↔ C ABI

- **FuncRef+Callback 蹦床的形参个数必须与 C 函数指针原型逐位相等**(2026-09-11 实测入档)。约定:C 以 `callback(closure, args...)` 调用,MoonBit 蹦床为 `fn(f, args...)`,首参 `f` 收到的就是 closure。
  - 案例:Table 的 `get_value`/`set_value` 蹦床比 C 原型各多带一位(4↔3、8↔6),形参错位后 ModelBox 被当函数指针调用 → 首次渲染直接段错误;而 `row_count`(1↔1)正确,所以行数正常、窗口能建——**部分接口"看起来能跑"会掩盖 arity 错位**,必须让每个回调都真实触发一次才算验证过。
- `extern "c"` 不能返回可空类型(ABI 与 C 指针不兼容,直接段错误):成败经 `Ref[Int]` 出参报告,句柄按非空返回。
- FFI 指针参数必须标 `#borrow`(编译器强制);同函数多参数写在同一个 `#borrow(a, b)` 里。
- extern 声明的控件参数必须写底层句柄类型 `View`,不能写 MoonBit 包装 struct(如 `Slider`):struct 经 ABI 传入的是包装对象而非句柄值,运行时全部"句柄无效"且被静默丢弃(Slider/Table 系列曾因此整体失效,2026-09 修复 22 处)。
- 闭包跨 C ABI:只允许无捕获的顶层函数字面量(编译为真实 C 函数指针);带捕获闭包走"函数指针 + 闭包指针"双参数模式(`on_click` 系)。
- 回调闭包由注册表保活(`yue/view.mbt`),窗口销毁后条目暂不回收——**已定案维持进程级保活**(2026-09-12):回调与窗口无归属关系可循,精准回收需 weak-reference 注册表,当前 MoonBit 生态不成熟;单窗口工具场景泄漏量可忽略(2026-09-12 定案)。
- **Toolbar / Vibrant 在 Linux 不可用**:libyue 头文件无平台 guard,但 Linux 静态库未编入任何相关符号(nm 实测零符号),调用会链接失败;Binding 侧已明确标注不暴露。
- **Browser::GetCookiesForURL 空 Cookie 列表会 FATAL**:libyue 0.15.6 内部 `CHECK(cookies)` 对空列表直接崩溃(上游缺陷)——WebKitGTK 以 NULL 表示空 GList 属合法结果,vendored mbt.6 及以前查询无 Cookie 的 URL 必崩;fork v0.15.6-mbt.7 已删该断言(空列表正常回调,与 Windows 端语义对齐),待资产出包回填后解除此限制。
- **改 shim 函数签名必须同批同步 `include/yue_mbt.h`,否则断链形态是"mangle 分裂"而非直观报错**(2026-09-19 实测):`yue_mbt.h` 全部声明包在 `extern "C" {}` 内,.cpp 定义经 include 继承 C 链接——头文件声明一旦漏改,.cpp 的新签名定义不再匹配旧声明,C++ 视为重载、按 C++ mangle 导出(`_Z26yue_mbt_view_on_mouse_down...`),而 MoonBit 生成端引用的永远是纯 C 名 → `undefined reference to 'yue_mbt_view_on_mouse_down'`。中间排查易被带偏:库内其余数百个符号都是纯 C 名(头文件继承),唯独新签名的几个是 mangled,nm 对比即可定位。修复=头文件与 .cpp、ffi.mbt 三处签名同批改齐(事件屏幕坐标扩充 7→9 参即踩此坑)。变体(2026-09-20 实测):新增 FFI 只写 .cpp 定义、头文件整段漏声明,同样 mangle 分裂——`yue_mbt_tray_set_image`(e0df994 引入)潜伏两个月无人触发,演示板补 `Tray::set_icon` 演示即断链;此形态比"改签名漏同步"更隐蔽(不是改漏,是整个没写),出包前用「cpp 全量函数定义 × 头文件声明」对照扫描可提前抓出(static/GTK 内部回调除外)。
- **SNI 托盘换图标"无效"的两层根因(2026-09-20 XFCE 真机实测)**:演示板「切主题图标/切回图片」两按钮均无视觉变化。①`set_icon_name` 已发 NewIcon 信号,但 item 上仍挂着程序内置的 IconPixmap——XFCE 面板 **IconPixmap 优先于 IconName**,重读属性后照画旧位图;修复=set_icon_name 时清空 pixmap(w=0/h=0/空字节),set_pixmap 时反向清 icon_name,保证两者互斥。②`Tray::set_icon` 的 SNI 分支是空操作(无 PNG 解码);修复=shim 新增 `yue_mbt_image_read_argb32`(GdkPixbuf 静态帧 → SNI 要求的 ARGB32 大端序,RGB 补 0xFF alpha,行距重排),MoonBit 侧 `Bytes::make(w*h*4)` 预分配后 #borrow 传入,读成功即 set_pixmap + NewIcon——头文件/cpp/ffi 三处同批写齐(防 mangle 分裂复发)。验证:真机点「切主题图标」托盘变系统终端图标、「切回图片图标」恢复 icon.png 位图。

---

## Linux

### 发行版

#### Ubuntu 24.04 LTS (Noble) ✅ 主链路实测

- 实测环境:24.04.4,内核 7.0.0-31-generic,X11 会话;GTK 3.24.41 / webkit2gtk 2.52.6(4.1 API)/ dbus-daemon 1.14.10。
- 系统依赖:`build-essential cmake pkg-config libgtk-3-dev libpango1.0-dev libfontconfig1-dev libx11-dev libwebkit2gtk-4.1-dev`。
- AppIndicator:24.04 已移除传统 `libappindicator3` 运行库,libyue 内置托盘不可用(内部 dlopen 失败只打日志、对象静默失效)→ 本项目用 `yue/traybus/` 纯 MoonBit SNI 直连面板替代。
- libyue 的托盘探测列表只认 `libappindicator3`;本机装有 ayatana 分支不代表可用,shim 侧已加空指针防御。SNI 后端上线后此路径仅作回退。
- webkit2gtk 包名 4.0/4.1 因发行版而异:`prepare.py` 用 pkg-config 探测,任一存在即可(4.1 需补 `-ljavascriptcoregtk-4.1`)。

#### 其他发行版 ❓ 未实测

- Debian(包名与 Ubuntu 接近)、Fedora(`webkit2gtk4.1-devel` 命名不同)、Arch 等待实测;移植第一步是核对各依赖的 pkg-config 名称,再跑 `prepare.py`。

### 桌面环境(托盘 / 菜单行为差异)

#### XFCE ✅ 实测通过

- 实测版本:Xfce 4.18.4(xfce4-panel 4.18.4),托盘插件 `panel-8-systray`。
- SNI watcher 在线:托盘图标、Activate 点击回调贯通;`Tray::on_click` 正常。
- 右键菜单:面板**自己镜像 DBusMenu 渲染**,不走 SNI `ContextMenu` 让应用自绘(实测 dbus-monitor 只有 `AboutToShowGroup` + `GetLayout`/`GetGroupProperties`,无 `ContextMenu` 调用)。
- **xfce4-panel 4.18(libdbusmenu 客户端)只发批量版 `EventGroup` / `AboutToShowGroup`,不发单条 `Event` / `AboutToShow`**(2026-09-11 实测入档):只实现单条版会被 UnknownMethod 静默拒掉,表现为菜单弹出正常但点击全部无效。dbus-monitor 抓真总线才能发现。
- 桌面识别:`XDG_CURRENT_DESKTOP=XFCE`,`@traybus.desktop_name()` 可用于诊断。
- **桌面通知走 `Notification::Show()`,不是 `NotificationCenter::AddNotification`**(2026-09-12 实测入档):libyue Linux 的 `AddNotification` 只把对象登记进内部列表,**从不发 DBus `Notify` 调用**;发通知必须调 `Show()`(内部 GDBus 异步 `org.freedesktop.Notifications.Notify`)。shim 曾在 Linux 分支只调 AddNotification,表现为通知静默失败无任何报错。判据:`dbus-monitor "type='method_call',interface='org.freedesktop.Notifications'"` 抓真总线,`member=Notify` 计数为 0 即调用未发出;修复后实测 xfce4-notifyd 正常弹气泡(含 actions 按钮)。
- **全局快捷键是 X11 `XGrabKey` 排他注册,被占用即返回 -1**(2026-09-12 实测入档):XFCE 自定义快捷键(xfconf-query `/commands/custom/`,如本机 `<Primary><Alt>s` 绑了钉钉切换脚本)已抓取的组合键,再注册会 BadAccess,libyue 用 error trap 吞掉后 `Register` 返回 -1——不崩溃但静默失败,使用方必须检查 -1 并提示换键。验证键位占用:`xfconf-query -c xfce4-keyboard-shortcuts -l -v`。
- **焦点在桌面时 xfwm4 抢占键盘,全局快捷键不触发**(2026-09-12 实测入档):`XGrabKey` passive grab 已挂、焦点在任意应用窗口时按键正常触发;但焦点落在桌面(xfdesktop)时 xfwm4 的键盘处理抢先,事件到不了应用。自动化验证用 `xdotool windowfocus <窗口>` 先把键盘焦点移入被测窗口再 `xdotool key ctrl+alt+<k>`,仅 `windowactivate` 不转移键盘焦点,会得到"没触发"的假阴性。

#### GNOME ✅ 实测通过(Ubuntu 24.04 原版,Wayland 与 X11 双会话,2026-09-15)

- 实测环境:ubuntu-24.04 虚拟机,GNOME Shell 46,`ubuntu-appindicators@ubuntu.com` 扩展默认启用(`gsettings enabled-extensions` 显示 `@as []` 是"默认值"表象,实际生效),`org.kde.StatusNotifierWatcher` 由 gnome-shell 持有。24.04.4 全新安装(原版桌面,默认 Wayland 会话;改 `/etc/gdm3/custom.conf` `WaylandEnable=false` 可切 Xorg 复测,两-session 均通过)。
- 托盘图标:顶栏正常渲染(IconPixmap 32+16 双档);SNI watcher 在线。
- **【修复】Menu 属性空菜单返回 `/` 导致 GNOME 点击图标全程无反应**(2026-09-15 实测入档):现象是图标显示正常但点击无菜单也无 Activate、dbus 零调用。根因:GNOME AppIndicator 扩展在**注册瞬间**就读 `Menu` 属性构造 DBusMenu 代理,而消费方 `Tray::new` 与 `set_menu` 之间有时间差,当时 `menu_items` 为空、属性返回根路径 `/` → 扩展代理指向无效对象(`journalctl` 可见 `UnknownObject: /`),菜单客户端永久坏死。XFCE/KDE/deepin 都是点击时才拉菜单所以不触发。修复:traybus 的 `Menu` 属性恒返回真实 `/MenuBar`(空菜单也导出,Qt 同款语义),后续靠 `LayoutUpdated` 通知填充。验证:dbus-monitor 见 Event/AboutToShow 流动、真机点击菜单三项回调闭环。
- 点击行为(GNOME 特有,`ItemIsMenu=false` 语义):左键/右键均发 Activate;实测菜单可弹出、项可点选,但弹出路径与 XFCE/KDE 不同(用户实测:菜单可开可点,退出闭环正常)。
- **全局快捷键在 Wayland 会话注册即段错误**(2026-09-15 实测入档):上游 `global_shortcut_gtk.cc` 直接用 `GDK_WINDOW_XDISPLAY(root)`(X11 专属宏),Wayland 下根窗口 impl 是 Wayland 类型,强转读出垃圾 `Display*` 传给 `XKeysymToKeycode` → SIGSEGV。修复:prepare.py `patch_linux_global_shortcut_wayland` 给 `Start/StopWatching` 与 `PlatformRegister` 加 `GDK_IS_X11_DISPLAY` 守卫,非 X11 会话 `Register` 返回 -1(既有失败语义)。X11 会话下 XGrabKey 行为不变。
- **鼠标键位是 yue 统一语义 1=左 2=右 3=中,不是 GDK 原始值**(2026-09-15 实测入档):libyue `ButtonFromGdkEvent` 把 GDK 的 2(中)/3(右)交换,全平台语义一致;showcase 菜单页曾按 GTK 惯例判 `==3` 为右键,导致"中键弹菜单、右键显示 2"(XFCE 同样存在,非 GNOME 特有)。
- 环境噪音(非本项目问题):spice-vdagent 在 Wayland 会话反复 SIGSEGV 弹 Apport 对话框(`/etc/default/apport` `enabled=0` 屏蔽);Wayland 空闲锁屏/息屏干扰自动化(`gsettings org.gnome.desktop.session idle-delay 0` + `lock-enabled false`)。
- SSH 远程起 GUI 进程的环境变量:X11 会话 `XAUTHORITY=/run/user/1000/gdm/Xauthority`;Wayland 会话 `XAUTHORITY=/run/user/1000/.mutter-Xwaylandauth.*`、`WAYLAND_DISPLAY=wayland-0`;两者都需要 `DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus`。SSH 环境缺 `XDG_CURRENT_DESKTOP` 会使 `desktop_name()` 返回 unknown(仅诊断信息,不影响功能)。

#### KDE ✅ 实测通过(Kubuntu 24.04,Plasma 5.27,2026-09-15)

- 实测环境:Kubuntu 24.04.5 虚拟机(X11 会话),面板托盘直收 SNI,新图标直接进可见托盘区(无折叠)。
- 全链路通过:图标显示 → 左键发 `Activate(x,y)`(回调贯通)→ 右键 Plasma 镜像渲染 DBusMenu(显示窗口/换图标/退出)→ 点「退出」回调触发 → `quit()` 干净退出(exit 0),图标即时消失。
- **协议行为与 XFCE 4.18 相反:KDE 发单条 `Event` / `AboutToShow`,不发批量版**(dbus-monitor 实证);traybus 两种都实现,无需分支。
- 环境准备:openssh-server 默认未装(GUI 终端 `sudo apt-get install openssh-server`);libwebkit2gtk-4.1-0 需补装运行库。

#### Deepin ✅ 实测通过(Deepin 23 社区版 + Deepin 25,DDE,2026-09-15)

- 实测环境:deepin 23 社区版虚拟机,glibc 2.38(Debian GLIBC 2.38-6deepin13),gcc 12.3,内核 6.6.84-amd64-desktop-hwe,X11 会话,`XDG_CURRENT_DESKTOP=DDE`。
- **dde-dock 实现 `org.kde.StatusNotifierWatcher`**,SNI 直连可用;新图标默认收进 dock 右侧「应用托盘」折叠区(点 `^` 展开),用户可拖出到常驻区。
- 全链路通过:折叠区图标显示 → 左键 `Activate` 回调贯通 → 右键 dde-dock 渲染 DBusMenu(三项齐全)→ 菜单点击回调 → 干净退出(exit 0),图标消失。协议为单条 `Event`。
- **宿主机二进制直接可跑**:Ubuntu 24.04(glibc 2.39)构建的探针在 deepin 23(glibc 2.38)运行正常——二进制 GLIBC 符号上限恰好 2.38,且 deepin 23 带 webkit2gtk-4.1 运行库(ldd 全解析);跨发行版分发不必逐环境重编,先查 glibc 符号需求(`objdump -T | grep GLIBC_`)。
- deepin 23 仓库**没有 openssh-server 包**(被引用但无可安装候选),远程管理走 virtiofs 共享 + GUI 终端执行脚本(安装器建的用户 noahliu 可 sudo)。
- 深度终端的 `script` 是 util-linux 标准版,注意命令须经 `-c` 传入(`script -q -f -c "cmd" log`),裸 `script -qf cmd log` 会报参数数错误。

- **Deepin 25(25.2)实测全链路通过**(Wayland 会话由用户确认;环境 glibc 2.38 / gcc 12.3 / 内核 6.6.143,`XDG_CURRENT_DESKTOP=Deepin`,宿主机二进制直跑):托盘探针折叠区图标/Activate/右键菜单/退出闭环。
- **DDE(23/25)下 Popover 气泡不可见且点击卡顿**(2026-09-15 实测入档):现象是点击「弹出气泡」无窗口出现、全局鼠标变钝。根因:libyue 的 Popover 是透明无边框窗 + `SetCapture` 指针抓取,DDE 合成器不渲染该透明窗、抓取又拖慢指针。修复:shim 检测 `XDG_CURRENT_DESKTOP`(23 为 `DDE`、25 为 `Deepin`,两种都要认),DDE 下回退无边框普通窗口(锚定控件 `GetBoundsInScreen` 正下方居中,不做指针抓取,点窗内关闭)。其余桌面仍走原生气泡。
- **deepin 25 移除了 openssh-server**(仓库无可安装候选):远程管理走 virtiofs 共享 + GUI 终端执行;deepin 23 同样没有该包。
- **`TextEdit::Delete()` 是删选区不是清空**(2026-09-15 实测):无选中时为空操作,「清空」语义要用 `set_text("")`(showcase 已改)。
- **showcase 在 DDE 的崩溃噪音**:DDE 剪贴板管理器交互时 libyue `Clipboard::Data` 构造报 `String data must be string type` CHECK(容错降级为 Text,不崩);`g_value_set_boxed` CRITICAL 为 GTK 与 DDE 主题交互噪音,不影响功能。

#### KDE / MATE / Cinnamon / Budgie / LXQt ❓ 未实测

- 协议层均支持 SNI(StatusNotifierItem),traybus 已按协议实现,预期可用;待逐一真实环境实测后在本节补充版本号与差异。

### DBus 线路协议坑(traybus 实测,桌面环境无关)

- DBus 数组长度前缀**不含首元素前的对齐填充**:算进去会被 dbus-daemon 判协议违规直接断连。
- DBus 头部 SIGNATURE 字段的 variant 签名是 "g"(u8 长度编码),按 "s" 编能过自洽单测但会被真实总线拒绝。
- **SNI `Menu` 属性必须恒返回真实菜单对象路径,空菜单也不能回 `/`**:GNOME AppIndicator 扩展注册瞬间即读该属性建代理,返回 `/` 会令菜单客户端永久坏死(详见「GNOME」节);Qt/ksni 同款语义是始终导出 `/MenuBar`。
- 教训:**单测证自洽,互操作必须上真总线验证**;discovery 类问题用 `dbus-monitor` 抓包定位,扩展/面板侧的 JS 异常看 `journalctl --user -u org.gnome.Shell@wayland.service`(gnome-shell 的 Gio.DBusError 行就是面板侧代理构建失败的第一现场)。

### 显示协议

- X11 ✅(当前唯一主链路)。
- Wayland ❌ 未支持(TODO 已列);libyue 的 GTK 后端以 X11 为准,迁移前不要在 Wayland 会话里验证 GUI 行为。

### GTK 相关

- Table(GTK)放进 Notebook 页签内会在尺寸测量时段错误(negative allocation),必须放普通容器或独立窗口(showcase 采用独立子窗口方案;2026-09-11 在 showcase 复现:崩前先出现 `Negative content width -1 (… owner GtkFrame)` 与 `GtkScrollbar` 的 `size >= 0` 断言)。
- **复合控件(Tab/Scroll/Group)在 yoga 树里是"无 measure 函数的叶节点",外框尺寸必须显式给出(flex/宽高),否则塌缩**。实测(2026-09-14,Ubuntu 24.04 + XFCE,showcase 声明式重写后):把 `Tab` 从直接作窗口内容(`SetContentView`,不进任何 yoga 树)改为挂进根 `Container` 后,整窗口签内容空白、页签以下不可交互,日志 `gtk_box_gadget_distribute: assertion 'size >= 0' failed in GtkNotebook` + `Negative content width -1 … owner GtkFrame`。根因三层:`Tab::Tab()` 构造时经 `UpdateDefaultStyle()` 把当时的 `GetMinimumSize()`(空 notebook ≈ 页签头高度)固化进 yoga minWidth/minHeight;`AddPage` 只设 View 层 parent、**不刷新该值**(libyue 上游局限);无 flex 时叶节点高度就停在固化值,notebook 页区域 = 分配高 − 页签头 ≤ 0。修复:给 `tab()` 节点 `("flex", 1.0)`(yoga 只管外框;页签页不进 Tab 的 yoga 树——`AddPage` 不做 yoga 插入,每页容器各自是独立 yoga 子树的根,由 GTK 分配页区域)。注意 `scroll_page` 曾误判为页内问题(490826b 对照实验),塌缩在 notebook 外框,与页内布局无关。
- **内容型控件的内容不走 `AddChild`**:`Group`/`Scroll` 直接继承 `View`(非 `Container`),挂内容必须用各自的 `SetContentView`;走 `Container::AddChild` 会被 shim 的 `CastTo<Container>` 类型校验拒绝(日志「类型不匹配，期望 Container，实际 Group/Scroll」),内容静默丢失。声明式层(`yue/declarative.mbt`)的 `group()`/`scroll()` 节点因此先把内容 mount 进一个临时 `Container`,再整块 `set_content`。切页瞬间的 `GtkNotebook` 断言在修复后仍少量残留(启动/切页瞬时布局噪音,CRITICAL 不致命),界面功能已全部恢复。
- **GTK 后端 NUContainer 的接缝缺陷(2026-09-14/15 实测,补丁见 `prepare.py patch_linux_container_events`)**。声明式重写把 Tab 包进根 `Container`、每页多包 `holder` 容器后暴露(旧版 Tab 直接做窗口内容视图,不经过这些路径):①`nu_container_map` 里事件窗口用 `gdk_window_show`(=map+**raise**),把覆盖容器全域的 INPUT_ONLY 窗口抬到子原生控件之上——X/GDK 命中被截走,**页签头点不动、滚轮失效,而按钮/键盘正常**(键盘走焦点,按钮/可编辑控件的窗口 map 更晚抬得更高);改 `gdk_window_show_unraised` 仅映射不抬高。②`nu_container_get_preferred_width/height` 硬编码返回 0,GtkViewport 以 child 的 size_request(上游被写为 0×0)计算滚动范围 → 滚动范围恒 0、滚动条不出现。**注意不要改成向 GTK 报告 yoga 动态自然尺寸**——allocate 会污染 yoga 状态,导致 requisition 协商震荡(实测 365→466→598→907 不收敛,布局停在中间帧,页面"有时不占满宽度");正确做法见③。③`Scroll::PlatformSetContentView` 对未挂载视图取 `GetPixelBounds()`=0×0 强制写入 size_request;改为:无显式 `SetContentSize` 时宽度保持 -1(随视口拉伸)、高度固化为内容 yoga 自然高度(经 `IsContainer()` 判别后向 `Container::GetPreferredSize` 查询)——滚动范围一次收敛且正确。`CreateEventWindow` 的 `attributes.y` 误写 `allocation.x` 笔误一并修正。
- **`Container::UpdateChildBounds` 开头的 `IsVisibleInHierarchy` 守卫让独立 yoga 根错过首次真实分配(2026-09-15 实测,同补丁函数)**。GTK 首次 size-allocate 发生在 map **之前**,此时整体可见性为 false → 守卫直接 return;map 后无人再以真实 allocation 重跑 yoga 布局,独立 yoga 根(每页 holder、Scroll 内容容器)永久停留在挂载时的自然尺寸布局——页内容"有时"不占满容器宽(是否必现取决于有无后续 resize 重分配)。修复:去掉整体守卫,布局计算无条件执行(`GetBounds()` 读的就是 size_allocate vfunc 已更新的 GTK allocation,提前布局安全;GTK 也允许对未映射 widget 预分配),孩子 bounds 传播仍受各自可见性限制;`nu_container_size_allocate` 里分配变化时补 `gtk_widget_queue_draw`。
- **`Slider::SetValue` 的 ignore 标记残留使滑块联动失效(2026-09-15 实测,同补丁函数)**:Linux 端拖动滑块,进度条与绑定标签不动。`Slider::SetValue` 无条件设 `ignore-value-change` 标记防回调循环,而 GTK 对"设置相同值"(yue 层 `Slider::make` 默认 `set_value(0)`,初值即 0)不发 `value-changed` → 标记残留,用户第一次拖动的首个回调被吞;xdotool 单点跳值场景恰只发一次信号,表现为完全失效。修复:仅当 `GetValue() != value` 才设标记。
- **`ProgressBar::SetValue` 的值域平台差异(2026-09-15 实测,修复在 shim)**:libyue Linux 端 `SetValue` 语义为 0..100(内部再 /100),yue 层统一 0..1 → 进度条只走到 1%(`v/100` 再被 /100)。换算分支已扩展为 Linux+Windows:Windows 端 ProgressBarImpl 未设 PBM_SETRANGE,控件默认范围即 0..100,2026-09-16 Windows 实测同型表现(进度条只走 1%)。
- **`View::GetBoundsInScreen` 在 Scroll/嵌套容器下坐标叠错(全 Linux 桌面,2026-09-15 实测,补丁 `patch_linux_view_bounds_in_screen`)**:上游实现手动累加各级 allocation,视口文档坐标混入——showcase 页面滚到中下部时锚点 y 实测 1221(屏幕仅 1080 高),气泡被定位到屏幕外,表现为「窗口最大化才显示气泡、非最大化不出现」。修复:改用 `gtk_widget_translate_coordinates` + 窗口原点(GtkX 原生感知 viewport 滚动与嵌套),原逻辑保留为回退。二轮修正(2026-09-16,XFCE 实测弹层盖住输入框):`gtk_window_get_position` 返回**含标题栏装饰**的窗口原点,与 translate 给出的**客户区相对坐标**混用,总偏移恰为一个标题栏高度+边框宽,弹层整体偏上盖住锚点控件——改用 `gdk_window_get_origin(gtk_widget_get_window(toplevel))` 取客户区屏幕原点。教训:GTK 屏幕坐标换算必须「客户区原点 + 客户区内偏移」,凡 get_position 与 translate 混用都会差一个装饰尺寸。所有屏幕坐标消费方(`popup_at`、气泡锚定)随之修正;新增 `ViewLike::get_bounds_in_screen` API(shim 4 个 ABI)。
- **表格 Checkbox 列指示器随行高缩放(XFCE 实测,2026-09-15,补丁 `patch_linux_table_checkbox_size`)**:GTK `CellRendererToggle` 的指示器在部分主题(XFCE)下不受 renderer `height` 控制——首版补丁只限 renderer 高度 20,showcase 表格演示窗口(未设行高)实测 checkbox 依旧填满整格;终版补丁对 Checkbox 列显式 `g_object_set(renderer, "indicator-size", 16)`(GTK 3.8+ 属性,默认 -1 即交给主题/字体),并保留 renderer 高度限 20,行高仍由文本列决定。补丁先 `g_object_class_find_property` 探测属性存在再设,防上游 GTK 变动。复现窗口(未设行高)截图像素级验证。

- **拖拽 FilePaths 数据被静默降级为 Text(XFCE 实测,2026-09-16,修复在 shim)**:shim 曾用 `Data(Data::Type::FilePaths, std::string)` 构造拖出数据,上游该构造只允许 Text/HTML,FilePaths 会被强制改写为 Text 并 NOTREACHED(即日志中 `String data must be string type` 的真正来源,此前误记为 DDE 剪贴板噪音);后果是拖出方实际提供文本而非 URI 列表,接收方按 FilePaths 请求得到空,表现为「拖入内容无法识别」。修复:shim 改用 `Data(std::vector<base::FilePath>)` 数组构造,并相对路径自动转绝对(`g_filename_to_uri` 不接受相对路径,绝对化用 g_get_current_dir 拼接)。**据此更正前文**:「String data must be string type」噪音在拖拽场景即此缺陷所致(剪贴板管理器交互场景另行确认)。showcase 拖拽演示同批改为:接收区分支显示 FilePaths/Text 可用性诊断;拖拽预览图 hotspot 补丁见 `patch_linux_drag_icon_hotspot`。
- **拖拽预览图垂在光标右下(Linux/GTK,2026-09-16,补丁 `patch_linux_drag_icon_hotspot`)**:上游 `gtk_drag_set_icon_pixbuf` 的 hotspot 写死 (0,0),预览图整体垂在光标右下;补丁改为图片宽高各半(中心对齐光标)。
- **拖放接收一律被拒(光标禁止、drop 不发生),先查 handle_drag_update(2026-09-19 XFCE 真机复验)**:components 拖放接收演示只注册 `handle_drag_enter` 返回 COPY,实测从文件管理器拖文件到接收区光标禁止、松手无反应——GTK 的接收判定在 drag-motion(即 `handle_drag_update`,拖拽每次移动触发一次),未注册时 nativeui 返回 0 即拒绝;enter 只是进入通知,**不决定能否接收**。修复:演示补注册 `handle_drag_update` 返回 `DRAG_OPERATION_COPY`(Windows 的 DROPEFFECT 同样由 update 决定,Linux/Win 通用);注册数据类型补 Image(从图片查看器/浏览器拖入的是图片内容而非文件路径,只注册 FilePaths/Text 时同样无数据可取);`handle_drag_enter/update/drop` 的 MoonBit 注释同步语义,使用文档中英拖放行同步。真机复验:拖文件显示「拖入 N 个文件」、拖图片内容显示「拖入图片」。
- **拖出发起两缺陷:文件管理器收不到文件 + 只能拖一次(XFCE 真机反馈 2026-09-20,修复在 fork v0.15.6-mbt.10)**:①`FillSelection` 的 `g_filename_to_uri` 只接受绝对路径、相对路径返回 NULL,URI 列表为空则目标端(Thunar/Nautilus)拿不到文件——fork 侧补 `g_canonicalize_filename(按 g_get_current_dir)` 兜底(shim 的 MakeFilePathsForDrag 早已绝对化,fork 层为双保险)。②「只能拖一次」的自洽解释:drag 会话异常终止时 `drag-end` 未触发,`priv->drag_context` 残留,`DoDragWithOptions` 开头 `if (drag_context) return NONE` 把第二次吞掉——补 `drag-failed` 回调防御性收尾(清 context + 退出嵌套 gtk_main,返回 FALSE 保留默认弹回动画);drag-end 改取 `get_selected_action`(suggested 在 end 时常为 0)。两修复真机复验不充分(现象不变),最终根因见下条(mbt.12);URI 绝对化双保险保留。
- **拖出根治:源端信号从未安装 + drag_context 未回填(XFCE 真机验证通过 2026-09-20,修复在 fork v0.15.6-mbt.12)**:mbt.10/11 两轮修复现象不变,受控复现(xdotool 合成按下/移动/释放,状态经 `write_text_file` 写文件通信——**MoonBit println 在管道/文件重定向下静默,插桩不能用 stdout**)推进到「ghost 已出现但接收方零响应」:drag-data-get/drag-end/drag-failed 回调一个不触发。铁证在源码:**源端信号只在 `RegisterDraggedTypes`(接收方 API)里安装,纯拖出场景从不调用它**——目标请求数据走 GTK 空 class handler(收不到文件,桌面高亮只是对会话的视觉反馈),drag-end 无人处理致嵌套 `gtk_main` 永不退出(「只能拖一次」真根因,即 context 残留但成因在信号缺失);加之 begin 后未回填 `drag_context`,即使信号有人听退出分支也不触发。修复(mbt.12):DoDrag 捕获 press 事件(`gtk_get_current_event`,须 GDK_BUTTON_PRESS 否则返回 NONE),武装 motion/release 处理器,过 8px 阈值后**以 press 事件**发起 `gtk_drag_begin`(X grab 须 press 时间戳,NULL 事件用 GDK_CURRENT_TIME 会被服务器自动按下 grab 拒绝,直接死因),拖拽图 begin 后设置,释放未过阈值按无会话收尾;DoDrag 内补装源端三信号(`drag_source_installed` 标志防与接收方路径重复连接),begin 成功即回填 `drag_context`。真机验证:Thunar/桌面落文件成功、连续多次拖正常。**教训:GTK 源端拖出必须自查 drag-data-get/drag-end 有监听(接收方 API 不会替源端装);begin 返回的 context 必须回填状态字段;API 语义=do_drag 须在按下回调(mouse_down)内调用,release/click 回调中调返回 NONE。**
- **GTK Entry/输入类控件慎用 `View::SetColor`(2026-09-16 实测)**:GTK 后端的 SetColor 会连带把 Entry 整体渲染为深色背景、文字不可见(override 语义覆盖 base 色而非仅前景);主题化输入框只应 `SetFont`,前景色保留系统样式。Button/Label 上 SetColor/SetFont 正常(组件库 button_t/label_t 依赖)。
- **主题组件不直接用原生 Button/Checkbox,交互行不挂子件(2026-09-16 实测,组件库 button_t/checkbox_t/radio_group 据此改全自绘)**:①原生 Button 设自定义底色后 hover 仍由 GTK 原生样式接管(深色遮罩盖掉自定义底),浅底变体的主题色字在深色 hover 上不可辨;②原生 Checkbox 悬停时主题画大号高亮圈,与直角低饱和主题冲突;③行级 hover 结构「父容器 on_mouse_enter/leave + 真 Label 子件」中,鼠标进入 Label 子件区域时父容器收到 mouse_leave,悬停效果在文字上消失。解法:交互项一律 Container 自绘(on_draw 画底 + draw_attributed_text 画字,on_mouse_enter/leave 切 hover 经 schedule_paint 重绘),文字不挂子件,hover 全区域生效。
- **GTK 端 `Painter::DrawAttributedText` 只实现 valign,水平 align 完全未实现(2026-09-16 源码确认,nativeui_jumbo_2.cc)**:实现里只按 valign 调整绘制起点后 `cairo_move_to` + `pango_cairo_show_layout`(pango 布局从该点向右下排布),`TextFormat.align` 被忽略,文字一律从区域左缘开始画;`draw_text` 内部即 `DrawAttributedText`,同样不受 align 影响。后果:所有「draw_text(align=Center)」在 GTK 上实际贴左——组件库 button_t 文字贴左、tag/avatar/steps/result/empty 的居中符号与文字全部偏左,固定方块(分页页码/头像/步骤数字)里的符号不居中;stateful_item 此前「居中正常」是 width=text_w+pad_x*2 两侧留白对称的巧合。修法:组件库统一手动水平居中——mount 时 `get_bounds_for` 测文本宽,绘制 x=(区域宽−文本宽)/2,align 保持默认,valign=Center 照常可用。
- **GTK 原生控件的边框/内阴影去除与样式化边界(2026-09-16 实测)**:Entry 等输入类控件的原生边框与内阴影由 GTK 主题绘制,`SetBackgroundColor` 只改底色改不了边框;shim 新增 `View::SetBorderless`(Linux 经全局 CssProvider 注入 `.yue-borderless { border:none; box-shadow:none; background-image:none; }` class),配合「外层自绘容器包原生控件」即可完全控制边框(含聚焦变色)/外边距/底色——组件库 `input_t` 即此方案(直角边框 + 聚焦主题色)。此前已证 GTK Entry 上 `SetColor` 会整体染黑,样式化只应 SetFont + SetBorderless + 外层自绘。
- **GifPlayer(GTK)不动画/不可见的三层修复(2026-09-15 实测,同补丁函数)**:①动画启动依赖 `IsVisibleInHierarchy()`(挂载于 Notebook 非当前页时为 false 跳过)与 `"show"` 信号(仅 `gtk_widget_show` 时发射一次,早于 SetImage 则错过)——切页 map 后无人启动 timer,永停首帧;补连 `"map"` 信号(每次实际映射发射,与 `"unmap"`→OnHide 对称,OnShow 幂等)。②drawing area 默认 no-window,嵌 NUContainer 链再进 Scroll 视口时 queue_draw 失效区域坐标归属错位——静止不重绘、整段空白、滚动时才留下一帧残影;改 `gtk_widget_set_has_window(TRUE)` 自建窗口。③**素材兼容性**:ImageMagick `convert` 生成的多帧 GIF 会让 gdk-pixbuf 的 iter 以约 1/9 速度爬行(纯 gdk-pixbuf C 程序可复现,advance 返回真但帧不轮换),换 PIL(pillow)生成即正常;且原素材本身是 45 字节损坏文件。GifPlayer 是 yoga 叶子且 `GetMinimumSize` 在默认 `ImageScale::Down` 下返回空,消费方需显式给宽高(showcase 用 `style=[("width",120),("height",120)]`)。
- **GTK Popover 弹出抢键盘焦点,自动补全无法退格(2026-09-16 实测,补丁 `patch_linux_popover_no_focus`)**:输入首字母弹层出现后,退格与继续输入全部失效。根因:上游 `Popover::ShowRelativeTo` 调 `window_->Activate()`(GTK 下 `focus_on_map=true` + `present`),键盘焦点被弹层窗口夺走,按键进不了锚点 Entry。修复:改为仅 `SetVisible(true)`(`gtk_widget_set_visible` 映射不带走焦点),保留 `SetCapture`(指针抓取,维持行点击与点击外部关闭);焦点留在输入框,退格清空后过滤结果为空自动收起。
- **autocomplete 弃 Popover 自管弹层,改挂 GtkEntryCompletion(2026-09-17)**:Popover 焦点补丁(上条)后用户复测仍反馈弹层回抢输入框 active;且自管弹层路线(Popover/替代窗口 + SetCapture + 失焦收起)每换一个桌面环境都要重新验证弹窗类型/指针抓取/失焦语义,收益比差。处置:Linux 自动补全整体换 `GtkEntryCompletion`——shim 新增 `yue_mbt_entry_set_completion`(候选串以 `\x1F` 连接过 ABI,match func 按 contains 字节子串过滤,与 MoonBit `String::contains` 同语义;`match-selected` 回调先 `gtk_entry_set_text` 回填再 invoke MoonBit 更新值),弹层是 GTK 托管的 popup 型窗口,不触碰 X 焦点,↑↓/回车/Esc 键盘导航原生自带;autocomplete 组件 Linux 分支不再创建 Popover/list,`Entry::set_completion` 在非 Linux 平台为空操作。代价:候选行为主题 TreeView 渲染,自绘 hover/行高不再适用(原生弹层圆角跟主题走,定制需 GTK CSS 通道,未做)。验证:moon check/test/build 全绿 + 启动冒烟;「输入/退格/点选全程焦点留在输入框」待真机复验。
- **autocomplete 移除,select_t 升级可过滤下拉;弹层「弃焦」硬化 + focus_in 开层推迟一拍(2026-09-18,待真机复验)**:需求收敛——EP Select filterable 交互(点字段弹候选、输入实时筛选、↑↓ 高亮、回车选中)统一由 select_t 承担(Linux 分支),autocomplete 组件连同 GtkEntryCompletion 通道整体删除(shim `yue_mbt_entry_set_completion` 及 match func、`Entry::set_completion`、ffi 同批移除)。两个关键技术点:①**XFCE 下弹层即使 ShowRelativeTo 不 Activate 仍会拿走焦点**(上上条补丁后复测仍回抢的机制解释:`gtk_widget_set_visible` 映射弹层窗口时,WM 按「聚焦新映射窗口」策略给焦,与 Activate 无关)——shim 新增 `yue_mbt_popover_set_accept_focus`:标记的弹层在 show 时从内容视图上溯顶层 GtkWindow 补 `gtk_window_set_accept_focus(FALSE)` + `gtk_window_set_focus_on_map(FALSE)`(后者必须在映射前设置,故放 show 之前),弹层出现与点击均不动 X 焦点;MoonBit 侧 `Popover::set_accept_focus(false)` 仅 select_t 可过滤分支调用,其余弹层(select/dropdown/color/popover_t)行为不变。②**focus_in(press 产物)里不能立刻开层**——「弹层打开一律在锚点 on_mouse_up」约定(2026-09-17 条)在可过滤字段上不可依赖:字段大部分面积是原生 Entry,其窗口盖在容器 INPUT_ONLY 事件窗口之上,容器收不到 Entry 区的点击;而 focus_in 时刻 release 还悬着,立即开层必被指针抓取判成点外闪关。shim 新增 `yue_mbt_call_delayed`(GDK 主循环下一拍,`g_timeout_add(0)`,待决输入事件处理完才执行),开层路径分流:输入 text_change / ↑↓ 键盘无鼠标待决直接开,focus_in 推迟一拍,容器与 Entry 的 on_mouse_up 兜底(已聚焦再点字段的展开入口)。键盘导航(↑↓ 循环高亮、回车选中、Esc 收起)沿用 autocomplete Windows 分支模式挂 Entry `on_key_down`,另挂 Entry `on_activate` 兜底(GTK 原生把回车送 activate 而不经 on_key_down 时两者只达其一)。验证:moon check(0 警告)/test 31/31/build 全绿 + shim 增量重编 + components 冒烟存活;「输入/退格/↑↓/回车/点选全程焦点不丢、弹层不闪关、二次点击可重开」待真机复验。
- **可过滤下拉真机复验两坑:原生 Entry 溢出自绘边框 + 弹层行点击穿透(2026-09-18 XFCE 真机反馈)**:①字段内嵌 Entry(字段高 30、上下 margin 4 → 分配 22)渲染溢出自绘边框——GTK 主题的 min-height(~30)经 `GetPreferredSize` 上报给 yoga,yoga 遵守 min 把子节点撑到 30+,原生窗口按自然尺寸绘制直接画出外层容器的边框外。修复:`.yue-borderless` CSS 追加 `min-height:0; min-width:0`(允许压进分配尺寸;input_t 等分配本就充足的场景不受影响),Entry 去上下 margin 占满字段高,文字由 GTK 单行垂直居中。**内嵌原生控件做「自绘框 + 原生输入」时,必须把主题 min 尺寸归零,且别用 margin 压缩分配空间**。②点候选行会穿透触发弹层正下方控件(实测触发 date_picker 开层):行 `on_mouse_down` 里 pick+close,close 隐藏弹层窗口即解除指针抓取,同一次点击悬着的 release 落到下层控件,而弹层类组件的开层入口恰是 `on_mouse_up`(2026-09-17 约定)→ 误开。修复:弹层内行的选中改挂 `on_mouse_up`(release 由弹层抓取正常派发给行,pick 后再无待决事件;按住拖出弹层松开不误选)。**弹层内可点项一律 release 处理,与「开层在 release」凑成一对约定**。验证:截图像素对比(无溢出、文字居中、与 DatePicker 一致)+ 冒烟存活;点选不穿透待真机复验。
- **「换系统主题会不会再顶爆」的根治:Entry 尺寸构造期归一化(2026-09-18,GTK 3.24 + Orchis 实测定案)**:上条用 `.yue-borderless` 补 `min-height:0` 的修法**无效**,且横向同病(170 宽字段给 Entry 分配 134 < 主题自然宽 166,原生子窗口盖住下拉箭头、探出边框 6px)。实测矩阵(纯 GTK 程序):①class 选择器(`.yue-borderless{min-height:0}`)与 `*` 通配**均压不过**主题 `entry{min-height:32}`,G_MAXUINT 优先级亦然;border/box-shadow/background-image 却能生效(同一条规则部分属性生效部分无效);②全局类型选择器 `entry{min-height:0px}` **有效**(高度塌到文字行高);③宽度存在 GTK3 硬编码 150px 下限,CSS 无论怎么写都压不过,唯一出口是 `gtk_entry_set_width_chars(n≥0)`(n×字宽+padding);④libyue 在**控件构造期**读自然尺寸钉成 yoga 最小值,此后改 CSS/width_chars 均不影响布局(运行期 API 全部落空)。修复:shim 新增 `yue_mbt_entry_normalize_metrics`(首次创建 Entry 时注册一次全局 `entry { min-height: 0px; padding: 0px 2px; }`,仅 Linux)+ `yue_mbt_entry_new_ex(type, width_chars)` 构造期钉宽(旧构造函数转发保兼容);`Entry::make` 加 `width_chars?` 参数(select_t 可过滤分支传 6),运行期 `Entry::set_width_chars` 因无效已删;entry_t 裸 Entry 自然高塌到行高后补显式 30(Linux 分支)维持观感。**效果:Entry 的布局尺寸完全由容器 yoga 分配决定,与系统主题解耦——换主题/换发行版不再顶爆自绘边框;颜色本就全自绘不受主题影响**。验证:moon check(0 警告)/test 31/31/build 全绿;截图像素实测字段四边 169/169 连续、箭头 22 像素落位设计坐标、右边框外零白像素;键入筛选/↑↓/回车/点选穿透待真机复验。
- **深色系统主题四连顶爆的根治:原生控件颜色整体接管(2026-09-18,深色主题实测)**:系统切深色主题后①多行输入框(GtkTextView)被刷成主题深底、②滚动条跟主题走深色、③输入框文字/placeholder 颜色错乱(白底白字/选区蓝底)、④弹层窗口底与边框深色。根因:此前只归一化了尺寸,颜色仍由 GTK 主题决定;且 libyue 弹层的边框/箭头色来自 `Color::Get(Name::Border/WindowBackground)`——fork 的 `gtk_theme.cc` 用合成路径渲染采样(`Border`=`GtkEntry#entry` 渲染 frame、`WindowBackground`=`GtkWindow#window.background` 渲染背景取均值)并**按名缓存**,故 CSS 对这两个路径的规则就是弹层配色来源。修复:shim `yue_mbt_apply_native_theme_css`(颜色 provider,G_MAXUINT-1——碾压系统主题但让位 borderless 的 G_MAXUINT;首调时并关 `gtk-overlay-scrolling` 让滚动条常驻钉色,3.24 起有此属性);默认浅色 CSS(label/entry 文字+插入符+底色+1px 边框/占位符/选区/textview 及其 text 节点/滚动条及 slider 含 background-image:none/弹层 window 底色)在首次创建 Entry 时随归一化一并挂载;`theme_apply` 每次用主题色板重建同一 CSS 整体覆盖(文字=text_primary、插入符=primary、选区底=primary_light、占位符/悬停=text_secondary、滚动条=border;文字底/弹层底固定白,与组件库硬编码自绘面一致)。**实测矩阵补充**:GTK3 里 class 选择器与 `*` 通配对 entry 尺寸类规则完全无效,唯 `entry{}` 类型选择器可覆盖主题;`min-width` 有 150px 硬编码下限,CSS 不可逾越。验证:深色主题下 components 表单页/诊断弹层截图——textarea 白底深字、placeholder 浅灰、选区主题浅蓝、滚动条浅色常驻、弹层白底无边框深环、箭头白色;moon check(0 警告)/test 31/31/build 全绿;冒烟存活。浅色/深色主题切换的长时运行回归待真机复验。
- **输入框溢出滚动的"变化内边距"无法关闭,只能缓冲+失焦回滚(2026-09-18,gtkentry.c 源码定案)**:用户观察"文本超出宽度后左右内边距随字符数变化"。查 GTK3 `gtk_entry_adjust_scroll`:`scroll_offset` 全程**像素级**(光标 x 像素直接推),无字符边界对齐、无任何 style property/CSS 开关;左缘"内边距"实为视口在字符中间裁切露出的半字符余量,随光标位置在 0~一个字宽(13px 字体约 8px)间变化——这是 GtkEntry 固有行为。缓解两招:①归一化 CSS 的 entry 水平 padding 2px→6px,裁切余量被固定内边距隔开,不再贴边跳变;②shim 新增 `yue_mbt_entry_set_position`(gtk_editable_set_position,仅 Linux),input_t/select_t 可过滤分支失焦时光标归 0——文本滚回首端,静态观感固定"左对齐+右缘裁切",编辑中(聚焦)才有滚动。注:undershoot 渐隐线(GtkEntry 内建溢出指示,UNDERSHOOT_SIZE=20,CSS 子节点 undershoot[left/right],经 background:none 隐去)与主题聚焦 outline(outline-width 归零)已在前两批处理,勿混淆三者。
- **`SetBackgroundColor` 的通配 CSS 连累 selection 子节点,选区白底白字不可见(Ubuntu 24.04 + XFCE,Orchis-Light-Compact 主题,2026-09-17 实测,补丁 `patch_linux_view_style_selection`)**:现象:`input_t`(Entry 设白底 + borderless)里选中文字后,选中高亮与白色背景重合,选中内容完全无法辨认。根因:libyue Linux 的 `View::SetBackgroundColor`/`SetColor` 经 `ApplyStyle` 以 G_MAXUINT 优先级把 `* { background-color: ... }` 挂到控件自身 style context——`*` 通配命中该控件节点树的**全部子节点**,包括 GtkEntry/GtkTextView 的 `selection` 子节点;选区高亮被刷成与所设底色同色(白),而选中文字的前景仍是主题定义的 selection 前景(Orchis 为白)→ 白底白字。修复:两处选择器改 `*:not(selection)`(`:not()` 接受类型选择器,GTK 3.24 实测解析通过;注意 `ApplyStyle` 把解析错误置 nullptr 静默吞掉,改 CSS 语法前必须先验证可解析),selection 恢复主题高亮色。关联:「Entry 样式化只应 SetFont + SetBorderless + 外层自绘」中 SetBackgroundColor 部分受本缺陷牵连,补丁后白底方案恢复可用;`SetColor` 的「连带渲染背景变黑」现象同机理待复验。验证:静态库重建 + moon check/test/build 全绿 + components 冒烟存活;选中可见性待真机复验。
- **libyue 无滚轮信号,canvas 自绘视图的虚拟滚动需 shim 补 GTK scroll-event(2026-09-17,shim 新增 `yue_mbt_view_on_wheel`)**:View 信号面只有鼠标按键/移动/键盘,没有 wheel;组件库 table_v_t(万行虚拟表格)需要滚轮。实现:Linux 对控件直连 `scroll-event`(`GDK_SCROLL_UP/DOWN` 映射 ±1,`GDK_SCROLL_SMOOTH` 传累计 `delta_y`),trampoline 返回 TRUE 消费事件防冒泡给外层 GtkScrolledWindow。两个坑:①libyue 自绘 Container(GTK)是 NUContainer,事件走自建 INPUT_ONLY 事件窗口,其 event_mask 默认不含滚轮——须 `nu_container_add_event_mask(GDK_SCROLL_MASK | GDK_SMOOTH_SCROLL_MASK)`(该函数会重建事件窗口);②`NU_IS_CONTAINER/NU_CONTAINER` 宏展开引用非限定 `nu_container_get_type`,只能在 `nu` 命名空间内用,shim 侧包了 `nu::container_add_scroll_mask` 小助手。ABI:`(view, invoke(closure, delta_y), closure)`;MoonBit 侧 `ViewLike` 新增 `on_wheel((Double) -> Unit)`。Win/mac 端滚轮接入待补(当前不挂回调、静默降级)。验证:moon check/test/build 全绿 + 演示板 1 万行冒烟存活;滚轮手感待真机复验。
- **万行级长列表不能用原生滚动容器(2026-09-17,组件库定案)**:X11 子窗口高度上限 65535(X 协议),3 万行 × 32px ≈ 96 万 px 远超上限,GtkViewport 方案(Scroll 包超高内容)在万行级直接不可行。定案:万行级一律整面 canvas 自绘 + 自管滚动(table_v_t 路线)——只画可见行窗口,滚轮/拖滚动条/键盘自管;原生 Scroll 只用于千行级以下。顺带:平滑滚轮(触控板)delta 是小数累计值,步进滚轮是 ±1,组件侧统一乘步长再钳制 offset。
- **Popover「按下不动松开即闪关」的交互缺陷(2026-09-17 真机复验,组件库侧规避)**:select_t/date_picker_t/dropdown_menu 弹层点击打开后,鼠标原地松开弹层立即关闭,按住拖动才保持。根因(上游 `popover_gtk.cc`):`Popover` 构造在自身窗口挂 `on_mouse_up`——光标不在弹层 bounds 内即 `Close()`(点外关闭);而 `ShowRelativeTo` 里的 `SetCapture()` 把指针抓取转移到弹层,press 发生在锚点控件、release 被弹层以 grab 身份收到,此刻光标仍在锚点上 → 判定点外 → 关闭。修复(组件库统一约定,**不改 vendor**):弹层打开一律在锚点 `on_mouse_up`(release 之后)进行,弹层出现时不存在待决 release,grab 语义干净;`popover_t` 的触发同样走 mouse_up。
- **Popover 的 `SetContentSize` 是硬分配,内容视图必须自带显式尺寸(2026-09-17 真机复验)**:date_picker_t 弹层只显示年月标题 + 星期行,42 格日期网格整体不显示。根因:`PopoverRootView::UpdateChildBounds` 把内容视图 bounds 设为「窗口尺寸 − padding/三角」,窗口尺寸由 `SetContentSize` 一次定死,**不读内容自然高度**;NUContainer 的 GTK preferred size 为 0(既有补丁保留行为),内容无显式高时实际分配不足。修复:弹层内容视图(calendar panel 等)一律写显式总宽高(yoga 定尺寸),`set_content_size` 与之匹配并留少量余量;色板/候选列表等同样按「项数 × 行高 + padding」显式给。**区间弹层再犯同坑(2026-09-20 真机反馈)**:新增的 time/datetime_range_picker_t 弹层又漏显式高度——时间面板与 datetime 的 body 组合容器没写 height,弹层内两行时间步进上下压塌(表现为「时间选择上下高度塌陷」)。修复:time 面板补显式高 88(上下 padding 16 + 两行 36)、datetime body 补 268×379(日历 258+分隔 1+时间面板 88+完成行 32),content_size 对齐 92/384。**新写弹层组件时逐容器自查显式宽高,这是硬约束不是建议**。
- **二轮复验三补(2026-09-17)**:①table_t 勾选框列容器无显式高度且行 `alignItems:center`,yoga 自然高 0 → 勾只露一条边;修为 `alignSelf:stretch` 拉满行高。**自绘指示器子容器要么给显式高、要么 alignSelf:stretch,别依赖空容器自然高**。②table_v_t 滚动时行绘制越过表头区(可见窗口只算行起止,没约束绘制区域);修为行循环外包 `painter.save()+clip_rect(0, head_h, w, vis_h)+restore()`——**自管滚动的自绘视图,内容必须裁剪出固定区**。③popover_t 的内容容器无显式尺寸,Popover 窗口立不住(与 content_size 硬分配同根因);修为容器显式宽高。
- **Windows 弹层「下拉用后卡死」(2026-09-17 真机反馈,静态定位)**:select/dropdown/color/autocomplete 的弹层根容器无显式尺寸时,Win 替代窗口(无边框置顶窗)与内容尺寸协商会布局震荡死循环(NUContainer preferred=0,与 GTK requisition 震荡同族);而 date 面板有显式宽高故正常。修复:**凡 Popover 弹层根容器一律显式宽高,且与 `set_content_size` 公式严格一致**(autocomplete 复用列表在 refresh 时同步)。若复验仍卡死,shim 的 Windows popover 已带 stderr 日志(`popover: show at …`/`popover: autoclose`),按「重定向日志→复现→取尾部」流程定位。
- **弹层根容器尺寸必须「创建期」给定,事后 set_style 会被分配时序吞掉(2026-09-19 XFCE 实测,上条 173 的升级认知)**:取色器色板弹层整体不绘制(白板,仅左缘每行 1-2px 竖线残迹),但格子事件窗口在(能点击看不到);on_draw 探针证明 grid/row/cell 的绘制回调一次都没触发。差异对照:date_picker 日历面板(正常)的宽高在 `Container::make` 创建时写死,色板 grid 是加完子件后才 `set_style` 补宽高——弹层根(PopoverRootView)的内容分配发生在内容挂载时,事后补的显式尺寸赶不上那次分配,yoga 停在 0 尺寸、绘制轮跳过内容。修复(组件库固化,使用者无感):**color_picker_t/dropdown_menu/select_t(非 Linux 分支)弹层根容器一律创建期写死宽高**,公式与 `set_content_size` 严格一致(select 的 300px 上限钳制一并前置);select_t 可过滤分支的动态高度保留 refresh 期 set_style(初始尺寸创建期已给,动态更新实测有效)。教训:**「显式宽高」不够,还要「创建期」——挂载后补的和没有一样**。
- **弹层点外关闭失效的根因在 libyue GTK 的 SetCapture(2026-09-19 源码定案,修复随 fork v0.15.6-mbt.5 发布)**:现象=可过滤下拉弹层打开后不选任何项、点外部不收起(仅 Esc 可关)。根因链:组件为防「按下不动松开即闪关」经 `call_delayed`(GDK 主循环 timeout 回调)开层,而 `Window::SetCapture`(GTK)第一行 `gtk_get_current_event_device()` 在无当前事件时返回 NULL → `gdk_device_get_source(NULL)` 报 `GDK_IS_DEVICE` 断言(此前日志反复出现、一直被当噪音)→ `if (!device) return` **静默放弃指针抓取**;弹层失去「release 到弹层窗口→光标在 bounds 外→Close」的唯一通路,且点空白处不引起 Entry 失焦,两条关闭路径全断。修复(fork `2f530bd3`,tag v0.15.6-mbt.5):device 为 NULL 时回退 `gdk_seat_get_pointer`(弹层自身窗口所在 display 的默认 seat)。moonbit-libyue 侧:LIBYUE_VERSION 升 v0.15.6-mbt.5(六资产 sha256 钉死),prepare 全绿。**教训:GTK 断言噪音不是噪音,每一条都对应一条静默失败路径**;另外 CLAUDE/GN 构建链要求 `python` 命令存在(Ubuntu 装 python-is-python3)。
- **carousel 右箭头点击多不生效(2026-09-17 真机反馈)**:面板容器用固定宽(= 舞台宽参数),但舞台被两侧箭头各占 24px,实际仅 272px——面板横向溢出 32px 盖住右箭头的事件窗口(溢出可见且事件窗口参与命中),点击多落在面板上。教训:**横向排布中兄弟控件旁的内容容器用 flex/stretch 填满,别用外层同宽的固定值**;溢出盖事件窗口的命中顺序不保证「后添加者在上」。
- **日历「周六」列被裁一半(2026-09-17 真机反馈)**:面板显式宽给了内容需求 252(7×36),但 yoga 的 width 是 **border-box 语义(含 padding)**——加 padding 8×2 后内容区只剩 236,最后一列被裁 16px。教训:**显式 width + padding 的组合,width 必须写「内容需求 + 两侧 padding」**;此前高度公式已含 padding、宽度漏了,同一处公式两种口径就是这种 bug 的形状。
- **取色器弹层「弹了但看不见」(2026-09-17 真机反馈,实证定位到锚点贴底越界)**:color_picker 在表单页深处,滚动后锚点贴近窗口底,弹层固定往锚点下方弹 → 越出工作区不可见。定位过程可复用:①临时 debug 例编程触发弹层 + 读 get_bounds——布局全部正常(grid 176×112/每格 26×26 在位);②程序化截图 + 像素统计(import -window root + PIL 数色块像素)——渲染正常。排除布局/绘制后唯一剩下的就是弹层窗口位置。修复(EP 同款行为):**锚点下方放不下时翻转到上方**——fork 的 `Popover::ShowRelativeTo` 补工作区判断(fork v0.15.6-mbt.2),shim 的 Windows 替代弹层(MonitorFromPoint + GetMonitorInfo 工作区)与 macOS 桩(nu::Screen)同步对齐,三平台一致。**注意**:moonbit-libyue 侧需等 fork 的 prebuilt CI 出包后把 LIBYUE_VERSION 升到 v0.15.6-mbt.2 并补 sha256,Linux 弹层翻转才生效(Win/mac 走 shim 即时生效)。
- **原生 tooltip 深底深字不可读,颜色接管 CSS 补 tooltip 规则(2026-09-19 XFCE 反馈)**:现象=tooltip 悬浮提示背景色与文字色不协调(深色系统主题下深底深字)。根因:tooltip 走 `View::SetTooltip`(GTK 系统提示窗口),其底色/文字色来自系统主题的两处独立配置,组件库的自绘面与既有颜色接管 CSS 都覆盖不到这个 GTK 内部延迟创建的独立窗口。修复:接管 CSS 加两条规则 `tooltip { background-color:#303133; border-radius:3px; }` 与 `tooltip label { color:#FFFFFF; }`——screen 级 provider 对 tooltip 窗口同样生效;恒深底白字不随主题色板(EP 语义,深浅主题下都可读),shim 默认串(`kDefaultNativeColors`)与 MoonBit 侧 `apply_native_theme_css` 两处同步。Windows/macOS 的 tooltip 为原生样式(黄底黑字/系统样式)本就可读,不动。验证:check/test/build 全绿 + 冒烟存活;悬停观感待真机复验。
- **bind_label 回显在 hbox 里被挤成两行(2026-09-19 反馈)**:现象=「最近点击:docs」「最近操作:菜单项 2」等点击回显换行成两行(链接/日期选择等多处)。根因:`Label` 默认富文本 `wrap=true`,Yoga 给 hbox 子节点按剩余宽度测量,文本超宽即按换行尺寸分配——动态变长的回显文本一超剩余宽就被压成两行。修复:`bind_label` 挂载时用 `wrap=false` 的 `AttributedText` 整体替换(`Label::SetAttributedText`),此后 `set_text` 仍在同一富文本上改写、单行测宽持续生效,宽度恒为全文本自然宽。两个语义注意:①`SetAttributedText` 会把 Label 的 `use_system_color_` 置 false,富文本路径不再吃系统文字色,须显式设色(bind_label 钉主题常规文字色);②替换时对齐选项重置(原 Label 默认 Center/Center),替换体按需给 align/valign。验证:check/test/build 全绿 + 冒烟存活;各回显处观感待真机复验。
- **可过滤下拉第三轮真机验证仍两处失效,特性移除(2026-09-19 真机反馈,决策收口)**:现象=①弹层打开后无法继续键入(筛选失效);②不开弹层直接在字段输入后,↑↓ 高亮与回车选中失效。至此「输入筛选 + 键盘导航」已三轮方案(autocomplete 自管弹层 → GtkEntryCompletion → select_t 可过滤 + accept_focus 弃焦 + call_delayed 推迟开层),每轮真机都在 X11 弹层焦点/指针抓取语义上翻车——弹层窗口(替代弹层)与锚点 Entry 的焦点归属在 WM 层不受完全控制,组件侧补丁只能逼近。决策:**砍掉可过滤形态,select_t 三平台统一只读点选**(点击弹列表、点选回填、失焦收起),select_t_filterable 整体删除;select_row 恢复无高亮标记的简单形态。保留资产:`Popover::set_accept_focus`/`Entry::new_ex(width_chars)`/`entry.set_position`/`call_delayed` 等 shim 通道与 API 保留(通用能力,ABI 不动);经验沉淀:凡「弹层内键盘交互」需求,先按只读/点选设计,键盘导航待上游(或自管 tooltip 式 overlay)方案成熟再引入。
- **clearable 清空按钮的实现约束(2026-09-19,组件库定案,真机待复验)**:input_t/textarea_t 的 ✕ 画在外框自绘层,右侧清空带必须**恒定**从原生子窗口(Entry/TextEdit)的 margin 让出、不随 ✕ 显隐变化——原生子窗口按 yoga 分配定位,运行期动态改 margin 的重排时机与自绘刷新不同步,✕ 会被子窗口盖住、点击带漂移(与「弹层根容器尺寸须创建期给定」同族:自绘装饰避开原生子窗口的布局都要一次定死)。select_t/date_picker_t 全自绘无此约束,✕ 画在箭头左侧,其点击判定在 on_mouse_up 里前置、优先于开弹层。清空语义:input/textarea 直接清文本(text_change 正常流转,textarea 的 on_input 收空串),select 置空串 + on_change("")(选项本身为空串时与「无值」同义),date 置 None(on_change 签名无空值,不触发)。验证:check(0 警告)/test 31/31/build 全绿 + 冒烟存活;真机 ✕ 显隐/点击/与弹层交互待复验。
- **nu::Label 控件默认水平居中,满宽拉伸的静态文字要用 label_t 或显式 set_align(Start)(2026-09-19 真机反馈)**:现象=side_menu_sections 组标题显示在侧栏中间而非左侧。根因:`nu::Label` 构造默认 `TextFormat{align: Center}`,声明式 `label()` 不改对齐;侧栏 vbox 的子项被 yoga 拉到满宽,居中就在整行宽度上生效(自然宽度=文本宽的场景看不出,满宽拉伸场景文字飘到中间)。注意与「DrawAttributedText 水平 align 无实现」(自绘路径)区分:控件路径的 align 是**有效**的,只是默认值是 Center。修复:组标题改用 label_t(恒左对齐,Secondary 角色与原字号/颜色一致)。约定:组件库内满宽布局里的静态文字一律 label_t 或显式 set_align(Start)。
- **暗黑模式适配的架构定案:主题订阅链,而非重建界面(2026-09-19,组件库)**:theme_apply 现在整体替换色板后**通知全部订阅者**(新增 pub `on_theme_change`),三类颜色各自收敛:①draw 回调现取 theme_* 的自绘组件——库内 `Container::make` 统一换 `themed_container`(构造时注册 schedule_paint),切换即重绘(含日历格子等弹层子件);②挂载期 `set_background_color` 定死的底色(面板/字段/列表/语义浅底/禁用底)——`set_panel_bg`/`bind_bg` 订阅重设(set_background_color 是静态属性,repaint 不会改它);③挂载期 `set_color` 定死的文字色(label_t/link/collapse/tree/card/bind_label)——订阅重设,注意 **AttributedText 改色不自带重绘,须手动 schedule_paint**(Label::SetColor 内部会 SchedulePaint,AT 直改不会)。色板新增 `bg_page`(页面底)/`bg_panel`(面板底):dark 下 page=#141518 深、panel=#1E2126、斑马 #232629 略浅于 panel,层次成立。Linux 原生 CSS 的窗口/输入框/多行底色从写死白改为跟色板——弹层窗口底色经 `Color::Get(WindowBackground)` 采样路径自动跟随(此前「颜色接管」机制的直接收益)。焦点环从双层 2px 主蓝减为单层 1px 主色 40% 透明(`theme_focus_ring` = primary 拼 "66" alpha;主色须不带 alpha)。验证:check(0 警告)/test 32/32(新增深浅切换订阅白盒)/build 全绿 + 冒烟存活;深浅切换的全量观感与原生控件跟随待真机复验。
- **active 反馈弃用主题色,全中性灰(2026-09-19 真机反馈定案)**:用户反馈焦点环即便 40% 透明的主题蓝也不要——**主题蓝只用于主要按钮/选中态等强调语义,active 类反馈(焦点环、字段聚焦边框、输入光标 caret)一律中性灰**。实现:`theme_focus_accent()` = text_secondary(浅主题 #6B7480 / 深主题 #9AA0A6,两主题下均与普通边框可区分又不抢眼),焦点环默认色与六处字段聚焦边框(input_t/input_number/textarea_t/select_t/date_picker_t/dropdown_menu/color_picker 字段)统一走它;Linux 原生 CSS 的 caret-color 同步从 primary 换 text_primary。附记:nu::Color 解析确认支持 #RRGGBBAA(8 位,alpha 最低字节),此前 40% 透明写法本身有效,弃用是语义决策而非解析缺陷。验证:check/test/build 全绿 + 冒烟;观感待真机复验。
- **主题订阅链第一轮真机暴露「残留旧色」+ 系统深色联动(2026-09-19 真机反馈)**:深色截图暴露三类残留:①**statistic 数字/标题、descriptions 键值、alert 文字、form_item 键、page_header 标题、tree/transfer/table 单元格等十余处 Label 定死色漏订阅**——上批改造只覆盖了显式 `set_background_color` 与部分 `set_color`,这批统一收敛到 `bind_fg(v, color_of)`(前景版 bind_bg,挂载即设+订阅重取),表格 cell_view 的 color 形参升级为 `color_of : () -> String` 闭包;②**timeline 色点/badge_dot 的 draw 闭包捕获挂载期色值字符串**——draw 现取才能跟主题,捕获值是挂载期快照,badge_dot 改为空串参数=默认语义红 draw 现取、显式传色恒定;badge_count 同病同修——不再复用 tag(其 color 参数为固定色语义,挂载期固化),改为自绘现取 theme_danger();③「draw 现取色但停留旧帧」的兜底(见下条定案)。**tooltip 白底残留**:接管 CSS 的 `tooltip` 选择器没赢过系统主题的同节点规则(Orchis 用 `tooltip.background` 类选择器,特异性更高),扩展为 `tooltip, tooltip.background, window.tooltip` 三连并直接在容器节点上声明 color(label 继承)。**系统深色联动**:shim 新增 `yue_mbt_system_prefers_dark`(GtkSettings 深色开关 + 主题名含 "dark" 兜底判定,小写化子串)与 `yue_mbt_on_system_theme_change`(监听 notify::gtk-application-prefer-dark-theme 与 notify::gtk-theme-name,信号只挂一份、后注册者立即回调一次;回调用**具名函数**——G_CALLBACK 是宏,lambda 参数列表逗号会被预处理器劈开,实测即报 macro passed 2 arguments),MoonBit 侧 `system_prefers_dark()`/`on_system_theme_change(f)`(app.mbt);demo 启动跟随系统 + 系统切换实时联动,「切换深浅」按钮保留为手动覆盖。Win/mac 的系统偏好读取待接(当前恒 false)。验证:shim 增量重编 + check(0 警告)/test 32/32/build 全绿 + 冒烟存活;深浅切换全量观感待真机复验。
- **「文字颜色切换后停留旧帧」的根因定案:GTK 子窗口内容不因失效自动重画,须逐窗口同步 process_updates(2026-09-19,程序化截图+像素统计定位)**:现象=theme_apply 后背景(CSS,XSetWindowBackground 即时生效)变了,所有文字/自绘面停留旧帧,唯有真实窗口 resize 后恢复——颜色设置链(MoonBit 订阅→shim→nu::AttributedText)全程正确,问题纯在重绘通道。探针矩阵(shim 侧 stderr 日志 + 窗口级截图 + PIL 像素统计)逐一排除:顶层/逐控件 `gtk_widget_queue_draw`、`gdk_window_invalidate_rect`(递归)、`gtk_widget_queue_resize`、`gtk_window_resize(±1)` 全部无效——失效排队后帧时钟的绘制没有落到这些子窗口;`yue_mbt_repaint_all` 终版=**遍历整棵控件树,对每个带 GdkWindow 的控件 `invalidate_rect` 后立即 `gdk_window_process_updates(gw, TRUE)` 同步走完 expose→draw→flush**(与用户实测「resize 后恢复」同机制:强制子窗口内容重画)。修复终版=`theme_apply` 里 post_task 推迟一拍 + repaint walk 逐控件 invalidate + `gdk_window_process_updates(gw, TRUE)` 同步刷。**但第三轮真机复测点击路径仍残留**——本地 xdotool 点击复现后逐层二分(调换角色顺序)定位到真正根因:**`AttributedText::SetColorFor` 只改 PangoAttrList 内容,不触发布局重排,重绘时 Pango 仍按缓存的旧属性渲染;哪些控件碰巧被无关事件(悬停/滚动)触发过重排,哪些就「看起来修复了」——这就是此前残留集合漂移不定、定时器路径与点击路径表现不一的统一解释**。修复=所有运行期 set_color 的订阅回调里,设色后**同文 set_text 一次**(`Label::SetText` 走 `pango_layout_set_text` 强制重排,新属性才被拾取;bind_fg/label_t/link/collapse/tree/card/bind_label 全部补齐)。教训:①**运行期改 Pango 属性必须同文 set_text 触发重排,改了不重排等于没改**;②排队式失效不可靠时逐窗口同步 process_updates 兜底仍然必要(两机制互补);③stdout 行缓冲与 stderr 无缓冲混流时日志顺序不可按字面读,程序化定位先重定向再对时序;④探针法可复用:临时示例 + xdotool 激活/点击 + import 截图 + PIL 聚类采样,验证后即删;⑤残留集合「随位置漂移」的随机性问题,优先怀疑**缓存失效缺失**而非绘制调度。验证:xdotool 点击切换浅→深→浅双向像素级确认(Section/Secondary/Body/按钮全对,无 resize)+ check(0 警告)/test 32/32/build 全绿 + 冒烟存活;demo 全量观感待真机复验。
- **颜色责任边界内敛定案(2026-09-19,清理轮)**:目标=使用方零颜色负担,坑全封装进库。收口动作:①**基础 `label()` 默认主题化**(bind_fg 主题常规色)——此前普通 label 走系统文字色,深浅切换不跟随,是使用方最容易踩的最后一个坑(label_t/bind_label/button 等此前已内敛);②**防坑助手 pub 化**:`theme_bind_fg(l, color_of)`(内部处理 set_color 后同文重排的平台坑)与 `theme_bind_bg(v, color_of)`(定死背景必须重设)供自定义组件一行接入;③自定义组件三法则入使用文档(自绘现取无需订阅/Label 文字用 theme_bind_fg/背景用 theme_bind_bg,固定色直接设)。注意:`label()` 的 handle 里用户 set_color 会被后续主题切换重置回主题色(订阅覆盖),文档已写明固定色场景用 label_t 角色或自行订阅。验证:探针确认普通 label 深浅切换正确变色;check(0 警告)/test 32/32/build 全绿 + 冒烟存活。
- **全组件 × 全坑清单系统审计(2026-09-19,56 个 pub 组件对 116 条经验逐项过)**:提取 13 项组件级检查项(自绘居中/Label 满宽对齐/hover 全区域/弹层五条/Entry 内嵌/避让带/border-box/兄弟容器 flex/指示器高/自管滚动裁剪/主题现取/色值捕获/原生控件替代)横向扫描+纵向核对。**清剿 13 处漏网**:①A 类「自绘 align=Center 贴左 / 字形图标」9 处——avatar 字母、steps 数字方块、calendar 月标题改**构造期/绘制期测宽手动摆位**;alert_closeable ✕、input_number +/−、transfer 与 calendar 的 ‹/›、backtop ↑ 共 5 组**字形图标全部矢量化**(draw_glyph_plus/minus/left/right/up 助手族,落实「字形图标一律矢量」既有定案;table_v CellTag 文本补 (tag_w−tw)/2 偏移);②D 类「弹层内点项须 release」4 处——select_row、dropdown_menu 菜单项、color_picker 色块格、日历日期格(date_picker 弹层复用;calendar_t 内联面板同语义无副作用),此前只读 select 恢复简单形态时丢了 pick_on_up 语义,press 时刻关层仍有 release 穿透风险。**待观察项(不盲改)**:table_t/tree/transfer 的行容器 hover 配 Label 子件,按 163 条③机制悬停高亮可能在文字区域断裂——用户从未反馈,事件窗口与子件窗口的 enter/leave 实际时序未定,真机观察后再定是否全自绘化。验证:check(0 警告)/test 32/32/build 全绿 + 双示例冒烟存活;各处居中/图标观感与弹层点选手感待真机复验。②stdout(行缓冲)与 stderr(无缓冲)混流时日志顺序不可按字面读,程序化定位先重定向再对时序;③探针法可复用:`examples/_theme_probe` 临时示例 + xdotool 找窗口 + import 截图 + PIL 聚类采样,验证后即删。附带:demo 中 Link「最近点击」/DatePicker「选中」/Switch「禁用」/Divider 上下/Card 内容/Collapse 内容等普通 label 改 label_t(普通 label 走系统文字色,不吃组件库主题——NULabel 是自绘件,CSS `label{color}` 对它无效,只有显式 SetColor 才有颜色)。验证:探针深浅切换像素级确认(按钮底 #E8F0FB→#1E3A5F、字→#5B8DEF、标题 #2A2F36→#E8EAED 全对)+ check(0 警告)/test 32/32/build 全绿 + 冒烟存活;demo 全量观感待真机复验。
- **滚动条 overlay 反转:从常驻钉色改悬浮式(2026-09-20,shim + yue 层双改)**:此前为让滚动条颜色跟随主题,shim 在 `yue_mbt_apply_native_theme_css` 里**全局关闭** `gtk-overlay-scrolling`,代价是滚动条常驻占位(13px)、内容区被挤压。反转原因:①GTK3 overlay 滚动条 idle 态已带 `overlay-indicator` class,主题 CSS 可完全控制三态(空闲透明 / hover 主题色 / dragging 深色),不再依赖常驻 classic 条;②用户明确要求"不占空间"。修复:①shim 把 `FALSE` 改 `TRUE`,注释反转;②`Scroll::make` 与声明式 `scroll()` 的 `overlay?` 默认改 `true`(声明式补上该参数,此前缺失只能 handle 变通);③`apply_native_theme_css` 补三条规则——空闲 `overlay-indicator:not(.dragging):not(.hovering)` 全透明(slider 4px 药丸)、hovering/dragging 时背景主题浅蓝 + slider 主题色 8px 圆角。验证:离屏探针 `gtk_scrolled_window_get_overlay_scrolling`=1、vbar classes 含 `overlay-indicator`、空闲宽度 13→0;moon check(25 warnings)/test 45/45/build 全绿;滚动条悬浮观感待真机复验。
- **侧栏菜单升级:图标 + 可折叠 + 选中强调条(2026-09-20,组件库)**:`side_menu_sections` 此前是"静态展开 + 组标题不可点 + 项无图标",观感与交互均弱。升级:①`icons? : Array[(String, IconKind)]` 给每页配图标,项 `on_draw` 里 `draw_icon` 自绘;②`foldable? : Bool = true` 组标题改自绘行(Secondary 色 + 右侧 Chevron 指示),点击 `set_visible` 切组内项容器,默认全展开,键盘 Enter/Space 同效;③选中态加左侧 3px 强调条 + 4px 圆角背景,图标与文字全在 `on_draw` 画(不挂子件,保持 hover 全区域不丢的约定);④`stateful_item` 同步扩展 `icon? / accent? / rounded?` 三参数,`side_menu` 单项版也支持图标。showcase 侧栏从五组重排为三组(组件 / 原生与自绘 / 系统能力),并配齐 15 个页图标。验证:moon check/test/build 全绿;侧栏折叠与选中观感待真机复验。
- **vendored 三平台不齐 → CI Win/Mac 链接断链(2026-09-20,实锤定位)**:CI Windows/macOS 报 `unresolved external symbol yue_mbt_tray_set_image / yue_mbt_image_read_argb32`(showcase.blackbox_test 链接期),本地 Linux 全绿。排查:`nm lib/macos-universal/libyue_mbt.a` 缺这两个符号(0 命中)——mac vendored shim 库停留在 mbt.6 时代(873b716「win/mac 待 vendor CI 拉齐」后一直没回填),而这两个符号是其后 1e396d1 才进 shim 的;fork 的 mbt.12 预构建库本身无嫌疑(报错符号全在 shim 侧,shim 是本仓库代码)。教训:**改 shim 后必须三平台 vendored 同步拉齐(推 vendor-* 标签走 vendor-native CI → --fetch 回填),只回填 linux 会让其他平台 CI 链接旧 shim 库**;本地「全绿」只覆盖本地平台,跨平台符号断链只有 CI 能暴露。修复:推 vendor-v0.15.6-mbt.12 标签重新固化三平台。


---

## Windows ✅ 首次实测通过

### Windows 10 / 11 ✅(2026-09-12 首测;2026-09-16 二轮实测通过:moon 0.1.20260911,主题组件库/键盘焦点全量 + shim 审计段首次 Windows 编译,hostshare 挂载构建)

#### 工具链准备(现象→根因→修复)

- **moon 原生后端要求系统 C 编译器**:PATH 上找不到 `cl/cc/gcc/clang` 直接报 "no system C compiler found"。修复:装 VS Build Tools(`Microsoft.VisualStudio.Workload.VCTools`),并在 **x64 Native Tools Command Prompt**(或先 call `vcvars64.bat`)里执行 moon/cmake。
- **libyue Windows 源码需要 ATL 头**(`base/win/atl_throw.h` → `atldef.h`),VCTools 工作负载默认不带:报 C1083 找不到 atldef.h。修复:VS Installer `modify --add Microsoft.VisualStudio.Component.VC.ATL`。注意 **quiet/passive 模式必须从提权进程启动**,否则立即退出且 Exit Code 5007(日志在 `%TEMP%\dd_installer_*.log`)。
- **`prepare.py` 下载 404**:发行包资产名与 `platform.system()` 不同名——实际是 `libyue_{v}_win.zip` / `_mac.zip`(不是 windows/darwin)。已修 `prepare.py`(ASSET_OS 映射),macOS 路径顺带修好。
- **hostshare/大小写敏感卷上编译 WebView2 头 C1083(2026-09-16 实测)**:仓库经大小写敏感的共享卷挂载到 Windows(Z: hostshare)时,libyue 的 `browser_impl_webview2.h` 写死 `#include <webview2.h>`(小写),而 SDK 只给 `WebView2.h`(大写 W)——普通 NTFS 大小写不敏感无感,敏感卷上 cl 直接 C1083。修复:`prepare.py` 的 `fetch_webview2_sdk` 解压后自动补 `webview2.h` 小写别名(NTFS 上无害)。同卷还有一个文件语义坑:`st_ino`/`st_dev` 不可靠,`shutil.copyfile` 内部 `os.path.samefile` 会把两个独立文件误判为同一文件直接抛 `SameFileError`(仓库根的 WebView2Loader.dll 与 SDK 内副本被判相同)——复制前先删目标绕开判定。同类坑:挂载卷上建 `.caseprobe_AAA.txt` 后按小写名查不到即敏感卷,构建 libyue 前先探。

- **`prepare.py` 的 Linux 补丁调用漏在平台分支外(2026-09-16 Windows 实测抓到)**:`patch_linux_drag_icon_hotspot()` 缩进错误落在 `if os_name == "Linux"` 之外,Windows 上 prepare 必崩(`FileNotFoundError: vendor\libyue\src\linux\...`,Linux 恰好能跑故一直未暴露)。修复:挪回 Linux 分支内。教训:平台分支补丁的新增调用务必确认缩进在对应 `if` 内;Windows 侧跑一遍 prepare 是发现此类问题的最快手段。
- **shim 的 `gtk/gtk.h` include 未加平台隔离(2026-09-16 Windows 实测)**:主题组件库的 `set_borderless` 用 GtkCssProvider 注入样式,实现已 `#if defined(OS_LINUX)` 隔离,但顶部 `#include <gtk/gtk.h>` 是裸的——Windows 上 MSVC 报 C1083 找不到 gtk/gtk.h(文件名含 gtk 的报错易误判为 GTK 依赖缺失,实际只是 include 没进分支)。修复:并入现有 `OS_LINUX` 分支。教训:shim 新增平台专属代码时 include 与实现要同一批隔离,只隔离实现不隔离头文件等于没隔离。

- **原生子控件在滚动容器里的四类 Windows 差异(2026-09-16 三轮实测)**:①滚动后原生 EDIT 的 HWND 不随容器实时移动,悬浮遮挡已滚上来的内容——`View::Layout()` 可强制重摆,`scroll()` 声明式封装已在 Windows 分支注册 `on_scroll` 强制重摆(shim 补 `yue_mbt_view_layout`);②输入框"内阴影"是 WS_EX_CLIENTEDGE 边缘样式,`set_borderless` 的 Windows 分支清该样式 + `SWP_FRAMECHANGED` 生效;③DatePicker 走系统主题按内容最小宽布局,不显式给宽只显示年份四位数(`set_style("width",130)`);④字体字形小图标(tree ▸/collapse +/−/checkbox ✓)在 Windows 渲染发虚且跨平台不一致——组件内一律改 Painter 矢量自绘(折线/实心三角),libyue Image 只支持位图无 SVG 解码,引入 SVG/字体图标库均不合算。


#### 链接参数(moon → cl/link 的真实行为,全部实测)

- **`cc-link-flags` 被 moon 原样拼进 `cl` 命令行**,不是直接给 link:GNU 风格 `-L/-l` 报 D9002/D9024;`/LIBPATH:` 也是 cl 不认识的编译器选项,只告警不转发。**正确做法是写链接输入**:`build/yue_mbt.lib setupapi.lib …`,cl 会把 .lib/.res 位置参数转交 link;系统库由 vcvars 注入的 `LIB` 环境变量解析,无需写参数。
- **分隔符必须用正斜杠**:`build\yue_mbt.lib` 会被 moon 的参数解析当转义吃掉反斜杠,link 收到 `buildyue_mbt.lib` 报 LNK1104。
- 官方 CMakeLists 的系统库清单本身缺项(无 user32/ole32/oleaut32/shell32 等),直接照抄会报 144+ 个 LNK2019(DefWindowProcW/VariantClear/SysStringLen 等);`prepare.py`/shim 清单已补齐 GUI 基础库,多余库 link 会忽略。
- **CRT 必须与 moon 一致为静态 /MT**:moon 生成代码固定 `/MT`(debug 亦然)。CMake 多配置生成器**忽略 `CMAKE_BUILD_TYPE`**,`cmake --build` 不带 `--config` 默认按 Debug(/MDd)编,最终链接报 LNK4098(MSVCRTD 冲突)+ 约 200 个 `__imp__*` 未解析。修复:shim 库 `cmake_policy(SET CMP0091 NEW)` + `CMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded`(须在 add_library 之前)+ 构建 `--config Release`(prepare.py 已自动化)。
- **moon 不因静态库更新自动重链**:cc-link-flags 与 MoonBit 源都没变时,换 `yue_mbt.lib` 后 `moon build` 报 "no work to do",需 touch 主包任一源文件强制重链。
- **CI 发布的 Windows exe 双击带控制台黑窗口(2026-09-20 修复,效果待真机复验)**:现象是 release-bin 三件套(hello/hello-themed/showcase)双击运行时 GUI 窗口旁常驻黑色控制台。根因在 moon 侧:其 Windows 后端链接模板(crates/moonbuild-rupes-recta 的 `link_executable_command`,moon 源码实测)把 cl 命令拼成 `cl /Fe… <objs> /link /nologo /subsystem:console /LIBPATH:… <传播的 link_flags> <默认库>`——`/subsystem:console` 硬编码,exe 按控制台子系统出厂,双击必附控制台。修复:prebuild.py 的 Windows link_flags 首位追加 `/SUBSYSTEM:WINDOWS`,传播机制恰好把它拼在硬编码 `/subsystem:console` 之后,link.exe 同名开关后者覆盖前者;入口点无需 `/ENTRY`——未显式指定时 link 按实际定义的符号自动选择,moon 产物的标准 `main` 对应 `mainCRTStartup`(`cl /subsystem:windows` + main 是既有惯例组合)。与首条 D9002 不矛盾:cl 只吃 `/link` 段**外**的编译器选项,本参数经传播落在段内直达 link。副作用评估:GUI 子系统下 stdout 在交互终端不可见(管道/文件重定向仍可用),moon test 的输出捕获走管道不受影响;Windows CI 只构建+测试不做 GUI 冒烟,链路无感。本机验证:Linux 分支输出不含该参数(零跨平台影响)、模拟 win32 分支输出以 `/SUBSYSTEM:WINDOWS` 开头、全量 check/test/build 零警告;最终效果待 CI 链接 + Windows 真机双击确认。

#### 应用清单(manifest)

- **进程启动即弹"无法定位于序数 345 于动态链接库 …exe"**:comctl32 的 `TaskDialogIndirect` 仅以**序数 345** 在 Common-Controls v6 导出,exe 无清单时加载旧版 comctl32,解析静态导入阶段就失败。官方 sample_app 靠自带 `exe.manifest` 解决。修复:官方清单编译为 `build/yue_mbt_manifest.res`(shim/CMakeLists 的 rc 自定义命令),经链接参数进入每个示例 exe(RT_MANIFEST 声明 Common-Controls v6 依赖)。运行验证:showcase 消息框(TaskDialog)正常弹出。

#### shim 平台差异(首次 Windows 编译暴露,均已条件编译修复)

- `dlfcn.h` 的 include 要按 `__linux__` 守卫(使用点本来就在 `OS_LINUX` 块内);MSVC 的 `M_PI` 需 `#define _USE_MATH_DEFINES`。
- **`base::FilePath` 在 UNICODE 构建下 StringType 是 `std::wstring`**,`FilePath(const char*)`、`+= path.value()` 全部编不过:统一经 `FromUTF8Unsafe/AsUTF8Unsafe` 进出(见 shim 的 `FilePathFromUTF8/FilePathValueToUTF8`)。
- **Windows 版 libyue 无 Popover**(发行包不含 popover.h,jumbo 源零实现):shim 保留 6 个 ABI 但降级为空操作/空句柄,MoonBit 侧 `Popover` 方法空转。
- 其余 API 面差异(对照发行包头文件的平台 guard 逐一修复):`Browser::Options` 无 `allow_file_access_from_files`(MAC/LINUX)/`hardware_acceleration`(LINUX);`Scroll::SetOverlayScrollbar`、`Clipboard::Type::Selection`、`Tray::SetTitle`(MAC/LINUX)不存在,均降级空操作;`NotificationCenter::AddNotification` 是 Linux 内部接口,Windows 走 `Notification::Show()`。
- `operator new/delete` 接管:Linux 用 glibc `__libc_malloc/free` 绕开 mimalloc 接管;**Windows 下 moon 以 `MOONBIT_ALLOCATOR=SYSTEM` 编译运行时**,CRT 堆即系统堆,重定向到 `malloc/free` 即可。
- **GTK 专属辅助/回调函数定义漏 `OS_LINUX` 守卫(2026-09-19 Windows 重建暴露,与 gtk.h 裸 include 同族)**:`yue_mbt_settings_notify`(深色偏好通知回调,注释写明仅 Linux)与 `yue_mbt_repaint_walk`(整树重绘遍历)的**调用处**都有 `OS_LINUX` 守卫,**定义**却裸放——Windows 编译报 C2065(GObject/GParamSpec/gpointer/GtkWidget 未声明)加连锁语法错,这两个函数所在的提交只在 Linux 验证过。教训:平台专属符号「定义与使用同圈守卫」,只守使用不守定义,在无该平台头文件的编译端必炸。修复:两处定义包 `#if defined(OS_LINUX)`;本次暴露的完整因果链(vendored 落后一代)见通用节 2026-09-19 条。

#### 运行期差异(showcase 真机逐页验证发现)

- **`AttributedText::SetFontFor/SetColorFor` 局部区间直接 CHECK 崩溃**(`nativeui_jumbo_2.cc`: "does not work on Windows"),只支持全文范围(0,-1)。修复:yue/painter.mbt 按 `platform()=="windows"` 对区间调用降级为无操作并告警一次(showcase 富文本页因此从启动崩溃变为正常渲染,区间样式按平台优雅退化)。
- **`Color::Get(Border)` 触发 NOTREACHED**(Windows 实现无 Border 分支,ERROR 日志且返回垃圾色):shim 对 Border 直接 `GetSysColor(COLOR_WINDOWFRAME)`,showcase 系统语义色行输出正常、日志零 CHECK。
- 托盘为原生后端:`Shell_NotifyIconW` 创建成功(日志"托盘:已创建");`set_title` 无对应概念为空操作;图标加载会有 libpng iCCP 警告(无害)。
- **字体发虚(GDI+ 灰度抗锯齿)**:libyue 的 GDI+ 画笔写死 `TextRenderingHintAntiAlias`(灰度 AA),Windows 上自绘的 Tab/按钮/标签小字明显发虚。修复:prepare.py 解压后对 vendor 做幂等文本替换(AntiAlias → `TextRenderingHintClearTypeGridFit`),重建即锐利;vendor 不进版本库,重跑脚本自动重新应用。
- **系统通知静默失败**:WinRT toast 的 notifier 按 AUMID 查找,进程未设置 `AppUserModelID` 时 `GetNotifier` 直接返回 null,`Show()` 静默失败。修复:shim 在首次通知前自动设置 AUMID(基于 exe 名)并写 `HKCU\Software\Classes\AppUserModelId\<AUMID>` 的 DisplayName;横幅是否弹出还受系统专注助手/全屏抑制影响,通知历史在操作中心可查。
- **浏览器优先 WebView2**:发行包 vendor 只带 WebView2Loader.dll 不带头文件,官方 CMake 也未启用;prepare.py 现从 NuGet 固定版本补齐 `WebView2.h`(sha256 钉死)并把 loader DLL 复制到仓库根(libyue 按 exe 目录→工作目录搜索)。shim 构建定义 `WEBVIEW2_SUPPORT` 并加 include;`Browser::new` 在 Windows 默认 `webview2_support=true`,loader/运行时缺失时 libyue 自动回退 IE。验证:本地 HTML(load_html)、ExecuteScript 正常;远程站点依赖系统代理可用。
- **demo:// 自定义协议在 WebView2 下静默无效**(注册/拦截链齐全但导航无效果,IE 引擎可用):上游待查;IE 兜底路径保留。
- **滚动区内容零高度(跨平台统一默认值原则)**:win32 的 Group/Scroll 不按内容自增长(GTK 有自然首选尺寸),showcase 里"滚动区里的文本编辑"整块塌陷。修复:给 Group 显式 `set_style("height", 128)`——同一默认值两端表现一致;遇到类似不一致一律用统一默认值吸收,不做平台分支。
- **声明式整页滚动失效(2026-09-14 实测,与 Linux GTK 同根因的 Windows 版)**:现象是各页签内容只能看开头、滚轮无反应、滚动条不出现。根因:Windows 的 `ScrollImpl` 用自绘滚动条,滚动范围只来自 `content_size_`,而它只能经 `Scroll::SetContentSize` 显式写入(初值 0×0);声明式 `scroll()` 节点内容高度动态、从不调 SetContentSize,范围恒 0。修复:prepare.py 新增 vendor 补丁——`scroll_win.h` 加 `content_size_explicit_` 标记,`ScrollImpl::Layout` 在未显式设置时向内容视图的 yoga 树实时查自然尺寸(`GetPreferredSize`),尺寸变化即重建滚动条;与 Linux 端 nu_container preferred 尺寸 + size_request 两处补丁同一思路。验证:showcase 各页签滚轮整页滚动、滚动条出现并随内容更新。
- **WebView2 页面一律报「无网络」(2026-09-14 实测)**:现象是 WebView2 引擎加载任意远程站点都出错误页,应用本身与直连网络正常(直连 curl 200)。根因:WebView2(Chromium)默认跟随系统代理,本机系统代理 `ProxyEnable=1` 指向 `127.0.0.1:7890`(Clash 类工具退出未还原设置),代理端口无监听 → 所有请求 ERR_PROXY_*;IE 回退路径走 WinInet 同样受影响。修复:libyue 未暴露 AdditionalBrowserArguments,prepare.py 补丁让 `GetWebView2Options` 读环境变量 `LIBYUE_WEBVIEW2_ARGS` 并 `put_AdditionalBrowserArguments` 注入——代理失效的机器设 `LIBYUE_WEBVIEW2_ARGS=--no-proxy-server` 即直连(见 showcase 网页页说明)。验证:设变量后 `moon run examples/showcase` 网页页完整渲染 moonbitlang.com(Chrome Legacy Window 确认 WebView2 引擎)。
- **按键键码与修饰键跨平台不一致(2026-09-14 实测)**:现象一,Windows 上按键显示的数字与 yue 常量表(VKEY_ESCAPE=65307 等)对不上,比较恒 false。根因:libyue Windows 的 KeyboardCode 是 Win32 VK 值(Esc=0x1B),常量表取的是 keyboard_codes_gtk.h 键值(Esc=0xFF1B);修复:`yue/events.mbt` 事件入口 `normalize_win_vk` 把 VK 码归一化到常量表(字母/数字/空格两表本就同值),并新增 `KeyEvent::describe()` 输出 "Ctrl+A" 形式可读描述。现象二,Ctrl+A 显示成 Alt+A、Shift+Enter 显示成 Ctrl+Enter。根因:libyue Windows 修饰键位是 `Shift=2 Ctrl=4 Alt=8 Meta=16`,shim 的 `NormalizeModifiers` 只有 Linux/macOS 分支做归一化、Windows 原样透传,位值撞上 MoonBit 层 `KEY_MOD_ALT=4`。修复:shim 补 `OS_WIN` 分支映射到统一的 `1/2/4/8`。验证:真机按 Ctrl+A 显示「按键 Ctrl+A(键码 65,Esc=65307 A=65)」,键码与常量一致、修饰键正确;纯函数部分白盒单测覆盖(`events_wbtest.mbt`)。
- **非正常退出留「幽灵托盘」图标(2026-09-14 实测)**:现象是进程崩溃/被关控制台/abort 后,托盘残留月亮图标,直到鼠标扫过才被 Explorer 惰性清理。根因:图标删除全靠 `nu::Tray` 析构时 `NIM_DELETE`;shim 的 TrayStore 全局持有引用,正常退出靠 CRT 静态析构链(Store 析构 → 引用归零 → TrayImpl 析构)兜底,异常退出这条链不跑。修复:shim 在首次创建托盘时捕获 TrayHost 属主窗口并安装四道进程级钩子(`atexit`/`SetConsoleCtrlHandler`/`SetUnhandledExceptionFilter`(链式传递前一过滤器)/`signal(SIGABRT)`,libyue 的 CHECK 失败走 abort),凡还有代码可执行的退出路径都按「属主窗口 + 图标 ID 区间」补发 `NIM_DELETE`——libyue 的 ID 从 2 起连续分配、只增不复用,对已删除 ID 是无害空操作。`taskkill /F`/TerminateProcess 式硬杀没有任何进程代码可执行,幽灵无法在进程侧根除,由系统悬停时惰性清理。验证:真机图标可见 → 关闭其控制台窗口 → 图标立即消失(鼠标移开排除悬停清理干扰)。另注:新托盘图标默认进溢出区,是否常驻可见区由系统/用户逐图标设置(设置 → 个性化 → 任务栏 → 选择哪些图标显示在任务栏上),应用无法强制。
- **托盘图标显示为空白不是引擎问题,先查图标资产(2026-09-14 实测)**:showcase/hello 的 `icon.png` 从建库起就是 1×1 占位图,libyue 按其像素拉伸到托盘尺寸(SM_CXSMICON),结果就是一团白——表现像「图标没渲染」,实为资产问题。修复:用 PIL 绘制抗锯齿月牙(512×512 绘制后 LANCZOS 缩到 64×64)替换两处。排查顺序:确认 Image 非空(shim 已有 IsEmpty 拦截)→ 看资产本身尺寸/内容 → 再怀疑 GetHICON/NIM_MODIFY 链路。
- **本轮复测还确认**:`moon test` 的测试驱动在安装新版 moon 后需 `YUE_MBT_SKIP_MANIFEST=1`(CI 同款,规避新版 moon 自带 MANIFEST 与 manifest.res 的 CVT1100);且 moon 不因静态库更新重链对测试驱动/showcase 同样成立——换 `yue_mbt.lib` 后要删 `_build` 下对应 exe 再跑,否则拿到旧链接产物误判(见上「链接参数」节)。
- **Popover 气泡在 Windows 的替代实现**:libyue 无 Popover(见下),shim 用无边框、不抢焦点、置顶的小窗口替代,弹在点击位置右下,8 秒定时自动关闭(无外部点击关闭钩子),`close`/`on_close` 语义保留;验证:showcase 气泡按钮弹出/自动关闭/日志闭环。
- **autocomplete 弹层点击即被激活,点选候选项落空(2026-09-17 代码审计推断,待真机复验)**:替代窗口的 `no_activate` 与显示时的 `SWP_NOACTIVATE` 只挡「弹出不激活」,挡不住「点击激活」——鼠标按下候选行时弹窗被激活为前台窗口,Entry 收 `EN_KILLFOCUS`,组件按失焦收起弹层,点击落空且焦点被抢。修复:shim `yue_mbt_popover_new` Windows 分支给弹窗 HWND 补 `WS_EX_NOACTIVATE`,点击直达行内容、窗口永不成为前台,焦点全程留在输入框。真机复验:点击候选行可正常选中,推断成立(2026-09-17)。
- **autocomplete 弹层定位到屏幕外不显示(2026-09-17 Win10 真机实测)**:输入弹层后无窗口出现,shim 日志 `popover: shown at (-595616576, 899)`——`View::GetBoundsInScreen` 在嵌套滚动容器下把垃圾偏移累进 x,与 Linux 同型 bug(Linux 已补 `patch_linux_view_bounds_in_screen`,Windows 上游实现同样不可信);y 值正常说明仅横向累加链坏。修复:shim `yue_mbt_popover_show_relative_to` Windows 分支锚点坐标改取原生子控件 HWND 的 `GetWindowRect`(Entry 是 SubwinView,`dynamic_cast` 取 HWND,系统真相不受布局累加污染;坐标系与 `SetWindowPos` 一致按物理像素),取不到 HWND 再回退 `GetBoundsInScreen`。真机复验:坐标正常但 libyue `Window::SetVisible(true)` 后 `IsVisible()` 仍为 0、窗口不上屏,最终显示靠直接 Win32(`SetWindowPos` 带尺寸 + `ShowWindow(SW_SHOWNOACTIVATE)`);libyue Windows 的 SetVisible/IsVisible 在该场景(无 Activate 的置顶替代窗)行为与 GTK 端不对等,待上游排查。
- **autocomplete 弹层关一次就再也弹不出(2026-09-17 Win10 真机实测)**:输入→点选/失焦收起后退格重新过滤,弹层不再出现。根因:shim 的 `close` 走 `nu::Window::Close()`,Windows 语义是**销毁窗口**(GTK 版是隐藏),自动补全复用同一弹层实例,销毁后 `show_relative_to` 拿到失效句柄,后续定位/显示全部静默失败(气泡 demo 每次新建 Popover 实例,故未暴露)。修复:`yue_mbt_popover_close` Windows 分支与 8 秒 autoclose 定时器改 `SetVisible(false)` 隐藏可复用,`on_close` 信号照 GTK 版 Close 语义手动 `Emit` 补发。
- **Windows 替代弹层无键盘交互,自动补全无法 ↑↓/回车(2026-09-17 真机实测)**:GTK 的 GtkEntryCompletion 键盘导航原生自带;Windows 弹层是自绘列表+焦点保持在输入框,`↑↓/回车/Esc` 无人处理,鼠标点选之外不可键盘操作。修复:autocomplete 组件 Windows 分支在 Entry 上挂 `on_key_down`(焦点一直在输入框,事件可达):↑↓ 移动高亮(循环,首列从无高亮起 Down 到第一项/Up 到最后一项)、回车选中当前高亮项、Esc 收起;行增加键盘选中态底色(与 hover 同色,`Ref[Bool]` + `schedule_paint` 重绘)。注意:`View` 裸类型无 `schedule_paint`(方法提升仅具体控件类型有),候选行数组需存 `Container`。
- **Windows 单行输入框文字贴顶不居中(2026-09-17 真机实测)**:Win32 单行 EDIT 文字从 client 顶部绘制,**没有任何垂直居中样式**(EM_SETRECT 仅多行有效,主流方案只有多行伪装/自绘/压高度);GTK/macOS 的输入控件原生垂直居中,同一段组件代码在 Windows 就露出差异。修复:autocomplete 组件在 Windows 把 Entry 高度 30 压到 22(13px 字体下文字近乎填满控件,视觉近似居中),其余平台维持 30。彻底方案(子类化自绘)成本高,未做。
- **Windows 原生控件暗色化探测定稿(2026-09-17 真机实测,examples/probe 探测)**:libyue Windows 端控件全部是真原生 Win32 通用控件(RTTI+窗口类名实证:Entry/TextEdit=RICHEDIT50W、Table=SysListView32+SysHeader32、DatePicker=SysDateTimePick32、Picker/ComboBox=ComboBox、进度条=msctls_progress32),**均不跟随系统暗色,须主动注入**,可行情汇总:①RichEdit:消息通道可行——`EM_SETBKCOLOR` 暗底+`CHARFORMAT2`(清 CFE_AUTOCOLOR)默认白字,已验证;②ComboBox:`Darkmode_Explorer` 本体可变暗但下拉列表不跟、样式粗糙,半吊子;③进度条:启用视觉样式时忽略 `PBM_SETBKCOLOR/BARCOLOR`,须先 `SetWindowTheme(h,"","")` 卸样式;④**Table 不可行**:libyue TableImpl 以 custom draw 逐行自绘白底,外部 `LVM_SETBKCOLOR/SETTEXTBKCOLOR/SETTEXTCOLOR` 每次重绘被其覆盖,Windows 版为预编译库无法改内部(远路:仿 Linux 补丁机制 patch Windows jumbo 源,列 TODO);⑤DatePicker/SysHeader32/原生 Button:Win10 无暗色通道,平台硬限制。**结论:暗色主题正式方案=组件库全自绘(table_t 自绘为暗色前置),原生控件暗色列为已知边界**;shim `yue_mbt_probe_dark` 与 probe 示例的「尝试暗色化」按钮保留作回归探针。
- **Windows 四轮组件实测修复(2026-09-16,均待 Windows 真机复验)**:①输入框内边距/内阴影不消失——shim `set_borderless` Windows 分支两处叠加缺陷:逻辑写反(`on=true` 要求去边框却**加上** `WS_EX_CLIENTEDGE|WS_BORDER`)+ **HWND 取法错误**(`GetNative()` 在 Windows 返回 `ViewImpl*`,直接 `reinterpret_cast<HWND>` 是野指针,`GetWindowLongPtrW/SetWindowPos` 全部静默失败——正确路径 `dynamic_cast<nu::SubwinView*>(v->GetNative())->hwnd()`——`SubwinView` 经 `Win32Window` 公开 `hwnd()`,仅原生子窗口控件有,自绘 Container 无 HWND 跳过;ViewImpl 层并无 hwnd(),首次修复误用导致 Windows 构建失败);另注意**上游 EntryImpl 创建样式是 `WS_EX_STATICEDGE` 而非 CLIENTEDGE**,清除需 STATICEDGE/CLIENTEDGE/WS_BORDER 三者一并;两处已修,此前多轮"清除 CLIENTEDGE"记录实际均未生效。②自动补全弹层内联展开把内容顶下去(非悬浮)——autocomplete 在 Windows 也走 shim 的无边框置顶小窗口弹层(此前仅 Linux 用 Popover);`show_relative_to` 弃用光标定位,改用 `GetBoundsInScreen` 锚定控件正下方(与 Deepin 分支同式);自动关闭定时器改固定 id(原 id=0 每次 show 新建计时器,旧计时器 8 秒后仍会误关正在使用的弹层)。③滚动后输入框钉在原位遮挡顶部内容、DatePicker 点击无法选中(疑同根因:原生 HWND 滚动后未随内容重摆,点击落空、键盘进不去)——`on_scroll` 信号在 `ScrollImpl::Layout` 重摆内容**之前**发射,回调内立即强制 Layout 会用过期的 `size_allocation` 定位原生 HWND;shim 在 Windows 把回调经 0ms 定时器推迟到本次滚动布局完成之后执行(修复在 `yue_mbt_scroll_on_scroll`)。
- **嵌套滚动区滚轮被外层抢走、虚拟表格滚不动(2026-09-17 Win10 真机反馈)**:现象两个——Backtop 演示的局部 `scroll` 区域滚轮无反应(外层页面滚);`table_v_t` 虚拟表格上滚轮也是外层页面滚、表格自身不滚。根因一个:Windows 的 `ScrollImpl::OnMouseWheel` 收到滚轮**直接消费并滚动自身**,不做光标命中下发(GTK 端滚轮由原生事件传播天然到达光标下最深控件)——整棵视图树里最外层 Scroll 永远第一个吃掉滚轮,嵌套 Scroll 与自绘 canvas 都拿不到;且 `yue_mbt_view_on_wheel` 的 Windows 分支本就是空操作(滚轮接入一直"待补")。修复两层:①prepare.py 新增 `patch_win_wheel_dispatch` vendor 补丁——`ScrollImpl::OnMouseWheel`/`ContainerImpl::OnMouseWheel` 先按 `FindChildFromPoint` 下发光标下子视图、未消费才滚动自身(顺带 `FindChildFromPoint` 由 private 改 protected),`ViewImpl` 增 `wheel_hook`(`std::function<bool(int)>`)钩子成员;②shim `yue_mbt_view_on_wheel` 补 Windows 分支,把 `WM_MOUSEWHEEL` 原始 delta(±120/格)换算成 GTK 语义(+1 下滚)挂到钩子上。验证:`moon check` 零警告、`moon test` 31/31、components 演示冒烟进程存活;滚轮交互待真机复验。
- **自绘文字"测宽手动摆位"在 Windows 全体偏左,result 图标最显眼(2026-09-17 Win10 真机反馈)**:现象是 result 大色块里的 ✓/✕ 明显偏左。根因:Windows 端 `AttributedText::GetBoundsFor` 的 `MeasureString` 用的是 **GenericDefault** 格式(带 overhang 内边距),而 `DrawAttributedText` 的 `DrawString` 用 **GenericTypographic**(不带),测出的宽度 > 实际绘制推进宽度,所有 `x=(区域宽-测量宽)/2` 式手动摆位必然左偏;GTK 端测量/绘制同走 pango 天然一致,故 Linux 上从未暴露。此前组件库头部注释「GTK 端 DrawAttributedText 不实现水平 align,水平居中必须手动测宽」是错误认知(`draw_text` 的 align 参数两端都实现,examples/drawing 早有全部对齐组合在用)。修复:组件库所有摆位用途一律改 `draw_text`/`AttributedText` 的 `align=Center/End` 交给平台布局引擎(result 图标、avatar、alert_closeable、steps 序号、calendar 三处、table_t/table_v_t 的 CellTag 与列对齐),测宽仅保留"算容器宽度"用途(tag 底色宽、页签头宽,背景稍宽几像素无视觉影响);组件库头部注释同步纠正,防止错误模式再被抄。验证:同上构建/冒烟,视觉待真机复验。
- 平台信息(`platform()=="windows"`、区域、缩放、屏幕)、剪贴板、定时器、全局快捷键注册、全局鼠标轮询、画布(GDI+)与浮动爱心窗口均实测正常。
- **右键菜单/弹层定位改用事件自带屏幕坐标,根治 GetBoundsInScreen 污染(2026-09-19 预防性修复,待真机复验)**:继 autocomplete 弹层(09-17 实测)后审计发现 `context_menu_for` 同样依赖 `GetBoundsInScreen`+相对坐标换算弹出位置,Windows 嵌套滚动容器下必踩同一垃圾偏移,且自绘 Container 无 HWND 可走 popover 的 `GetWindowRect` 绕行路。根治:shim 新增 `MouseEventScreenPoint` 在事件转换层直接提取屏幕坐标——GTK 取 `GdkEvent` 的 `x_root/y_root`(根窗口全局坐标);Windows 用 `View::GetWindow()` 顶层 HWND `ClientToScreen(0,0)` + `position_in_window` 合成(同为物理像素;**不取 `MSG.hwnd`**,它可能是原生子控件、与客户区偏移错位;mac shim 为纯 .cpp 无 root 坐标,回退 (0,0));随鼠标事件链路全量透传为 `MouseEvent.screen_x/screen_y`(5 个蹦床 7→9 参)。`context_menu_for` 改直接用 `e.screen_x/e.screen_y` 弹菜单。真机复验:components/showcase 基础页色块右键,菜单应弹在光标处。
- **Entry 创建在非 Linux 无限互相递归,窗口永远出不来(2026-09-19 Win10 真机实测,vendored 断链修复后的首个运行期暴露)**:现象:showcase/components 启动后无窗口、进程存活、单核 ~70% 空转、永不崩溃;最小复现=窗口内容含任意一个 Entry(单独 button/label/scroll/side_menu/menubar/tray/register_protocol 均正常)。定位路径:`Window::make` 本身不显示窗口(显示靠 mount_window 的 handle 回调末尾 `activate()`),把 activate 前置到 handle 第一句仍无窗口 ⇒ 卡死在更早的 `set_content(mount(...))`;showcase 逐页二分到「基础控件」页,再按控件二分到 Entry。根因:`56aa944`「entry 归一化」引入的 `yue_mbt_entry_new_ex` 在 `#else`(非 Linux)分支转调 `yue_mbt_entry_new_typed`,而后者的唯一实现就是转调回 ex——Linux 分支因真正创建控件无症状,Windows/mac 创建任何 Entry 即无限互相递归→栈溢出→被 shim 的进程级异常钩子(SetUnhandledExceptionFilter,幽灵托盘清理用)接住后**活锁**:异常处理自身再溢出,表现为「进程活着、CPU 空转、永不崩溃」,极具迷惑性。**编译器早就在报:MSVC C4717「函数将导致运行时堆栈溢出」正是这对函数,vendored 首次 Windows 构建时警告已出现,但该库从未在 Windows 运行过**——教训:C4717 类「逻辑必死」警告当错误对待。修复:`entry_new_ex` 非 Linux 分支直接 `new nu::Entry`(同 Linux 分支去掉 GTK width_chars 部分),重编后 C4717 消失。验证:重建 vendored + `moon clean` 重链(静态库更新 moon 不自动重链,见上),showcase/components 窗口均出现(MainWindowHandle≠0)、CPU 归零。方法论入档:①诊断 GUI「无窗口」,PowerShell `Get-Process <name> | Select MainWindowHandle` 是无头判定利器(0=无可见窗口,配合 CPU 区分「空转卡死」与「安静等待」);②stdout 重定向到管道时 MoonBit `println` 全缓冲,进程被杀缓冲即丢,「日志空」≠「没执行」,插桩别依赖 println;③showcase 窗口标题显示「Showcase — https://www.moonbitlang.com/」系浏览器页 IE/WebView2 宿主控件导航后改写宿主窗口标题的经典行为(浏览器页挂载即 load_url),Linux 无此现象,属化妆品级已知差异待真机复验。另,本文所记「显示靠 mount_window 的 handle 回调末尾 activate()」契约已于 2026-09-20 作废:hello-themed 漏写手动 activate 致「进程存活、窗口不出现」,mount_window 改为 handle 执行后自动激活显示,消费方无需再手动 activate。
- **输入框文字贴顶与不随主题,两问题一并根治(2026-09-19 修复,视觉待真机复验)**:①贴顶:Win32 单行 EDIT/RICHEDIT 自客户区顶部绘制,无垂直居中样式(09-17 已知,当时 autocomplete 压高 22 临时缓解,该组件已随 d9cfb39 重构移除)。根治方案(目标「原生输入框垂直居中,跨平台观感一致」,尺寸按实际字体度量推导不硬编码):组件层 `entry_vcenter`——用 AttributedText 测字体行高,把原生控件收窄到「行高(带原生边框时 +1px×2)」,剩余高度均分为上下 margin;接入 `entry_t`(新增 height 参数默认 30)/`input_t`(外框线 1px×2 从可用高扣,borderless 控件高=行高)/声明式 `entry`(Windows 拦截 height 样式走同分解,无 height 默认 30,并统一 Sans13 字体保证行高测量与控件实际字体吻合;其余平台样式原样透传)。②主题色:Windows 原生 RichEdit 恒黑字不随 theme_apply(Linux 由全局接管 CSS 覆盖,问题仅 Windows)。新增 `yue_mbt_entry_set_colors`(shim→头→ffi→`Entry::set_colors` 全链路):Windows 走 EM_SETBKCOLOR+CHARFORMAT2(清 CFE_AUTOCOLOR 启用 crTextColor;SCF_DEFAULT 只影响新文本,既有文本重设一遍按新默认色重排——probe 暗色实验验证过的通道);Linux 逐控件 GtkCssProvider(优先级 G_MAXUINT 盖过全局接管 CSS,provider 挂 widget 数据槽复用,主题反复切换不叠层);mac 空操作。组件层 `bind_entry_theme`:非 Linux 设 theme_text_regular/theme_bg_panel 并订阅 on_theme_change 重设。**MoonBit FFI 的 Bytes 参数须 `#borrow` 属性行(extern 声明前一行列参数名),写在参数位是语法错误**。验证:check/test/build 零警告全绿、两示例冒烟存活;垂直居中效果与深色主题下输入框浅字深底待真机复验(components 演示板即深色主题,输入区最直观)。
- **弹层/下拉点外收起补齐(2026-09-19 修复,交互待真机复验)**:Windows 替代弹层此前无点外关闭(仅 8 秒兜底 + 组件 focus_out,点非聚焦区域全不触发),所有走 Popover 的下拉(select_t/dropdown_menu/calendar_t/date_picker_t 等)表现为「必须点一个选项才能收起」,而 Linux GTK Popover 原生输入抓取自带点外关闭。修复(纯 shim,无 ABI 变更):弹层展示期间挂 WH_MOUSE_LL 低级鼠标钩子——按下点不在弹层矩形内(GetWindowRect,与 LL 钩子的 pt 同为物理像素)即经 `MessageLoop::PostTask` 收层(不在钩子内直接动 GUI 防重入;任务捕获触发时刻的弹层指针,执行前若已换了新弹层则不动它;不吞事件,CallNextHookEx 照常放行,点击仍送达它本来的目标);收层统一 `HideActivePopover`:摘钩+杀定时器+SetVisible(false) 复用窗口+按 GTK Close 语义 Emit on_close(已隐藏不重发——修掉「钩子收层与组件 focus_out 收层双发信号」),8 秒兜底路径同样补发 on_close(此前超时静默隐藏,组件状态不复位,潜伏「超时后再也打不开」)。附带修复 prebuild 潜伏 NameError:`except (SameFileError, ...)` 名字从未导入——**Python 仅在异常真发生时才求值 except 名单**,此 bug 潜伏数轮直到 WebView2Loader.dll 被占用真抛异常才炸出,改 `shutil.SameFileError`;另 moon test 的 CVT1100(测试驱动链 moon 自带 MANIFEST 与 yue_mbt_manifest.res 重复)本机复现,按 CI 惯例测试门槛带 `YUE_MBT_SKIP_MANIFEST=1`,应用构建不受影响保留 manifest。验证:33/33 + 双示例构建/冒烟存活;点外交互待真机复验(select/dropdown/日历/取色器打开后,点击页面空白、标题栏、其他应用窗口均应收起;点弹层行内仍正常选中)。
- **splitter 拖拽失效两连根因:显式 SetCapture 拆掉隐式捕获 + flexbasis 百分比字符串不解析(2026-09-19 修复,合成拖拽已验证,真机复验)**:现象:Windows 下 hsplit/vsplit 把手拖动无任何反应(初始比例也不对,把手贴左而非 50%)。诊断方法论:shim 三处加 stderr 打点(无缓冲;**MoonBit println 管道下全缓冲,taskkill 即丢,插桩别用**)+PowerShell 合成真实拖拽(GetProcess MainWindowHandle→GetWindowRect→mouse_event/SetCursorPos)+窗口截图前后对比(截图字节级相同=画面没变,一眼识破「看起来跑了其实没变」)。根因一:libyue Windows 端对挂有鼠标处理器的视图在**按下时自动隐式捕获**(EmitMouseClickEvent 的 implicit capture),组件再显式调 `SetCapture` 会触发 `WM_CAPTURECHANGED`,`OnCaptureChanged` 把 `captured_view_` 清空——**隐式捕获被自己人拆掉,系统级 ::SetCapture 还在但视图级路由已断**,拖拽中的 move 从此按光标位置分发给别的视图,把手再也收不到(probe 铁证:HasCapture before=1 / after=0 / 系统捕获=1)。修复:`yue_mbt_view_set_capture` Windows 分支加守卫——已持有捕获时跳过重复设置(GTK 端捕获为逐控件 grab 无此冲突,维持直调)。根因二:`flexbasis:"50%"` 百分比字符串仅 GTK 端解析,Windows 端静默忽略(首栏被压到最小宽,把手贴左);修复:splitter 初次布局完成后(post_task)按实际尺寸换算**像素**写 flexbasis,与拖动写像素同一形态,跨平台统一。验证:合成拖拽把手 50%→~68%(与 +120px 合成位移吻合),截图确认分栏实际变化;门槛 33/33+双示例构建/冒烟。**教训:Windows 端「显式捕获」API 与隐式捕获并存时,先查 HasCapture 再动 SetCapture;样式字符串(百分比/关键词)是 GTK 语义,跨平台组件一律用数值像素。**
- **弹层气泡/下拉背景不随主题(2026-09-19 修复,合成点击已验证)**:现象:深色主题下 popover_t 气泡在 Windows 上白底——文字色经 label_t 的 on_theme_change 重设本就跟随,白底浅字不可读;同类影响所有 Popover 承载面(select_t/dropdown_menu/calendar_t/date_picker_t/取色器)。根因:Linux 端弹层在应用窗口体系内,全局接管 CSS 的 `window { background-color }` 规则罩住弹层;Windows 的替代弹层是**独立裸 nu::Window**,不存在应用级 CSS,默认白底无人管。修复(全链路新增 `Popover::set_background_color`,shim→头→ffi→方法):shim `yue_mbt_popover_set_bg`——Windows 给替代小窗 SetBackgroundColor,Linux/mac 空操作(CSS 已覆盖);组件层 `bind_popover_bg`(设 theme_bg_panel + 订阅 on_theme_change 重设)接入全部 5 个 Popover 创建点。验证:暗色主题最小示例 + PowerShell 合成点击「打开气泡」,截图确认气泡深底浅字清晰可读、与主窗风格统一;门槛 33/33+双示例构建/冒烟。方法论入档:**工具产出的 .ps1 脚本一律用英文注释——无 BOM UTF-8 的中文注释被 PowerShell 5 按 ANSI 误读,会解析出错乱成诡异的 Bitmap 构造异常,排查极耗时**。
- **dialog 无法关闭且点击穿透遮罩:Windows 命中测试与绘制层级方向相反(2026-09-19 真机反馈,合成点击验证,修复随 fork v0.15.6-mbt.6 发布)**:现象:dialog_t 弹出后 ✕/取消/确定全部无反应,还能透过全屏遮罩点到主界面按钮(视觉遮罩存在、事件像不存在)。根因:libyue Windows 端 `ContainerImpl::FindChildFromPoint` 按**子视图添加顺序正序**遍历、取第一个包含点的子视图,而 `ContainerImpl::Draw` 同样正序绘制(后添加者画在最上层)——**命中优先级与绘制层级正好相反**:dialog/toast 这类后挂窗口根的 absolute 遮罩视觉盖全窗,鼠标命中却永远让给先挂的主布局容器,事件全部穿透到「下面」;面板内按钮同样收不到事件,关闭链路整体失效。GTK 端事件由 GDK 原生分发(上层 widget 优先),两端不一致,故 Linux 从未暴露。追溯:此前 carousel「右箭头点击多不生效」(100d008)实为同根因的另一症状(absolute 面板溢出区抢占箭头命中),当时从布局绕过未触及本质。修复:fork `51fabe6f`——`FindChildFromPoint` 改倒序遍历(`adapter_->ForEach(callback, /*reverse=*/true)`),命中顺序对齐绘制层级;无重叠兄弟不受影响(倒序首个命中与正序相同),重叠场景由后加者(视觉上层)优先,语义与 GTK 看齐。本地即时验证的编译路径(供后续 fork 补丁复用):**本机无 gn/depot_tools 编不了 fork 裸树,CMake 链路吃的是发行包 dist 结构且 nativeui 源在包内是 jumbo 合并文件(无散件可替换)**——下载上一版源码 dist、在 `nativeui_jumbo_*.cc` 里对同一函数打补丁、`cmake -S fork/scripts/prebuilt -DDIST_DIR=<dist>` 编出 yue.lib 回填 `build/yue_prebuilt.lib` 与 `lib/windows-x64/`;正式归档走 fork 提交+tag+CI 出包(prepare.py 版本与 sha256 随之升级,本地手工回填会被官方包覆盖对齐)。验证:diagdlg 最小例(后台按钮计穿透数)合成点击——遮罩打开时点「后台按钮」位置→遮罩收起(close_on_mask 生效)、穿透计数 0;重开点「删除」→收起且 on_confirm 触发;门槛 33/33+双示例构建/冒烟。**教训:凡「视觉在上层的视图收不到事件/点击穿透」,先查命中遍历方向与绘制顺序是否一致;GTK 正常不代表 Windows 正常,分发机制两端本就不同**。

#### 验证方式

- `moon run examples/hello`:窗口渲染 + 托盘 + 点关闭经 `on_close→quit()` 优雅退出。
- `moon run examples/showcase`:8 页签逐一点击(基础控件/输入与选择/画布/网页/对话框/系统集成/事件/富文本)、菜单(勾选/单选/表格独立窗口)、消息框、画布色相重绘、鼠标事件实时回显、全局鼠标、浮动爱心、关闭退出,全程日志零 CHECK 失败。
- 2026-09-14 复测(新版 moon + 声明式 showcase 12 页签):`moon check` 零警告、`YUE_MBT_SKIP_MANIFEST=1 moon test` 30/30 全过;真机逐页截图确认——整页滚轮滚动、滚动条出现;网页页 WebView2 完整渲染 moonbitlang.com(设 LIBYUE_WEBVIEW2_ARGS=--no-proxy-server);事件页按键显示 Ctrl+A/Esc,键码与常量表一致。
- **AttributedText 区间字体/颜色补齐(fork v0.15.6-mbt.9,2026-09-20)**:上游 Windows 实现直接 `CHECK(start==0 && end==-1)` 崩溃——GDI+ 只有单一 font/brush,无富文本。修复:自建分段布局器(补丁提交进 fork `54979f8b`/`39613ee9`)——①区间属性按插入序存 run(`PlatformSetFontFor/SetColorFor` 全文设置重置同类型区间,`SetText` 后区间钳制到新文本);②`LayoutAttributedText`:按 `
` 切逻辑行 → 区间边界切原子段(后设 run 按属性覆盖)→ 流式摆位(段放不下整段换行重排,行首放不下二分取最长前缀)→ 手动几何对齐(水平逐行/垂直整体);③`GetBoundsFor` 与 `DrawAttributedText` 在有区间属性时共用同一布局(测量绘制同源,无 runs 走原 GDI+ 直绘路径零回归)。**坑:`Gdiplus::Font::GetHeight` 重载是 `(const Graphics*)` 指针,传对象引用无匹配重载直接编译失败(mbt.8 CI win job 挂在这,mbt.9 修)**。MoonBit 层 `set_font_for/set_color_for` 的 Windows 降级守卫删除,`markdown_view` 等区间着色三平台一致。Windows 真机观感待复验。

---

## macOS ❓ 未实测

### macOS(libyue v0.15.6 发行包含 ARC / no-ARC 双库结构)

- CMake 已备 ARC/no-ARC 双库分支,均未验证;首次实测先确认两种链接形态哪条走通。
- 首次 macOS 实测以 CI 承担(2026-09-14 起,`.github/workflows/ci.yml` 的 macos job,macos-latest ARM64 Runner):构建 + 测试级验证,不做 GUI 运行冒烟(headless Runner 无 WindowServer)。实测结果待首跑后回写本节。
- prebuild 的 Darwin 分支已按官方构建结构**预修**(尚未实测):链接参数补第二个静态库 `-lyue_mbt_noarc`(no-ARC 库符号被主库引用,GNU ld 从左到右须排其后)+ 全部框架(AppKit/Carbon/IOKit/Security/WebKit/OpenDirectory)+ `-lobjc -lc++ -lpthread`——静态库的系统依赖不会自动传播到 moon 的链接命令行,必须显式给出(与 Linux 侧 pkg-config 补系统库同构)。
- 版本细分(按 macOS 大版本)待实测后补充。
- **shim 平台分支漏 macOS 守卫(2026-09-16 CI 首跑实测)**:环境 macos-latest(ARM64,Xcode 16.4/AppleClang 17)。现象:CMake 编 `yue_mbt.cpp` 报 20 个错误——`HWND`/`GetWindowLongPtrW`/`WS_*` 未定义、`gchar`/`g_get_current_dir` 未声明;libyue 源码本身只出弃用 warning(无害)。根因:①`yue_mbt_view_set_borderless` 的 `#if defined(OS_LINUX)` 后直接 `#else` 落 Win32 分支,macOS 被兜进去;②`CurrentDirForDrag` 的 `#else` 分支用 glib(Linux 才有 gtk/glib 头与 USE_GLIB 宏)。修复:borderless 补 `#elif defined(OS_WIN)`,macOS 空操作(NSScrollView 自身无边框);CurrentDirForDrag 拆三支,macOS 用 `getcwd`(顶层补 `#if !defined(_WIN32) #include <unistd.h>`);顺修 `yue_mbt.h` 块注释内 `yue/*.mbt` 触发的 `-Wcomment`。验证:Linux 本地 prepare + check + test(31/31)+ build 全绿;macOS 侧待 CI 复跑确认。
- **shim 调用 mac 版头文件不存在的 API(2026-09-16 CI 第二轮)**:`Window::SetSkipTaskbar`/`Window::SetIcon`/`App::SetID` 三处——发行包头文件里它们在 `#if defined(OS_WIN) || defined(OS_LINUX)` 块内,mac 无声明。修复:三处调用补同样守卫,mac 分支空操作(mac 窗口图标/应用身份随 Bundle 走,本就无对应语义);顺修悬空 `extern "C" //注释` 与下一行前缀重复触发的 `-Wduplicate-decl-specifier`(注意本文件无全局 extern "C" 块,各导出函数前缀不能顺手删)。排查方法:用 Linux 版 vendor 头文件的平台 guard 全量对照 shim 的 `->method(` 调用逐个核对——`SetOverlayScrollbar`(`#if !OS_WIN`)、`MessageBox::Show/SetTitle`(已有 mac 分支)、`SetMenuBar`(已有 mac 降级)均安全。

---

## 维护约定

1. 每次真实环境实测后,把「发行版 / 桌面环境 / 版本 + 现象 + 根因 + 修复 + 验证方式」写进对应小节;新发行版/桌面从 ❓ 占位小节开始。
2. 涉及协议互操作(DBus / DBusMenu / SNI)的结论必须来自真总线、真面板;单测自洽 ≠ 互操作通过。
3. 简短结论同步 README「Known Limitations / known pitfalls」;本文保留完整过程与版本细节。
